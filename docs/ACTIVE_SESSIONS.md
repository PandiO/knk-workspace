# Active Sessions

Live tracker of in-progress work across all four Knights and Kings repos
(`knk-workspace`, `knk-web-app`, `knk-web-api`, `knk-plugin`). Referenced by
`docs/ai-agents/GLOBAL_AGENT_INSTRUCTIONS.md`.

**How to use this file:**
- Before starting work, check the table below for anything overlapping
  your intended scope. If something overlaps, stop and flag it rather than
  proceeding.
- Add a row when you start; scope by **feature**, not by repo, since most
  features span all three component repos.
- Update your row's Status/Last Updated whenever you pause, and move it to
  "Recently Completed" (below) when you finish, rather than deleting it
  outright — keeps a short trail of what just happened.
- This file will get concurrent edits from parallel sessions. That's
  expected — if you hit a merge conflict here, resolve it by keeping both
  rows rather than picking one; conflicts on this file are cheap to fix by
  hand.
- Keep entries short — this is a status board, not a design doc. Link out
  to the relevant branch, PR, or `docs/reports/` file instead of restating
  detail here.

## In progress

| Feature | Repos / files claimed | Owner (human / session) | Status | Started | Last updated |
|---|---|---|---|---|---|
| _(example)_ Siege minigame — capture point sync | web-api: `Services/SiegeService.cs`; plugin: `siege/` package | Claude Code session A | Implementing capture-point event handling | 2026-09-17 | 2026-09-17 |

## Recently completed

| Feature | Repos touched | Finished | Notes / link |
|---|---|---|---|
| _(example)_ Codebase scan — knk-plugin | knk-plugin (read-only), knk-workspace (docs) | 2026-09-17 | See `docs/reports/plugin-scan-2026-09-17.md` |
| InventoryMenu Phase 1 — core data model & persistence | web-api: `MenuTemplate`/`MenuSectionTemplate`/`MenuItemTemplate`/`VariableBinding`/`ActionBinding`/`ConditionBinding` entities, EF migration, `MenuTemplatesController` CRUD, seed mechanism (`knk-web-api` branch `claude/inventory-menu-phase-1-wxl3z0`); plugin: `MenuTemplatesDataAccess` gateway, domain records, API-client DTOs/mapper/impl, config wiring (`knk-plugin` same branch name); workspace: `docs/specs/inventory-menu/IMPLEMENTATION_PLAN.md` | 2026-09-21 | Migration generated + applied end-to-end against a real local MySQL 8.0 instance and exercised through the running API (nested create/read/update/delete, validation errors) — caught and fixed a real EF fixup bug this way. Plugin side verified with direct `javac` against real dependency jars, since `repo.papermc.io` is blocked by this session's egress policy (confirmed via the proxy's own status endpoint, not routed around) and Gradle couldn't resolve `paper-api`; `KnkApiClient.getMenuTemplatesQueryApi()` confirmed present in compiled bytecode via `javap`. **Branch note:** the task originally named `knk-plugin`'s `InventoryMenus` branch, but that branch's history turned out to be rooted in a pre-restructure repo scaffold unrelated to current `main` (`git merge` refuses it as "unrelated histories"). Per explicit user decision, `InventoryMenus` is being left alone/ignored — `claude/inventory-menu-phase-1-wxl3z0` (forked cleanly from `main`) is the branch going forward for this feature on `knk-plugin` too. Not yet built/tested: knk-paper's own edits (`KnKPlugin.java`, `KnkConfig.java`, `ConfigLoader.java`, `DataAccessFactory.java`) could only be reviewed by hand against the `ItemBlueprints` precedent, since compiling them needs `paper-api` directly. Needs before Phase 2: a real Gradle build once `paper-api` is reachable, and any decisions Phase 2 (rendering engine) surfaces that Phase 1 didn't need to make. |
| InventoryMenu Phase 2 — rendering engine core | plugin: Bukkit-free runtime engine in `knk-core/.../core/menu/` (`RuntimeMenu`/`RuntimeMenuSection`/`RuntimeMenuItem`, `MenuTemplateAssembler`, `MenuSlotCalculator`, `MenuLayoutValidator`, `MenuSession`/`MenuSessionRegistry`, `MenuVariablePlaceholderText`) plus the Bukkit rendering/interaction layer in `knk-paper/.../paper/menu/` (`MenuRenderer`, `MenuItemBukkitMapper`, `MenuService`, `MenuClickListener`, `MenuLifecycleListener`, `OpenMenuContext(Registry)`), wired into `KnKPlugin` + a `/knk menu open\|page` dev-harness command (`knk-plugin` branch `claude/inventory-menu-phase-2-x4y98c`, forked from Phase 1's now-merged-in branch); web-api: additive `example.pagination` seed template so pagination has something real to page through (`knk-web-api` same branch) | 2026-09-21 | Built in two sessions with different network access, worth noting for future sessions: the first (cloud sandbox) had `repo.papermc.io` blocked, so verification there was 232 real JUnit tests via a standalone `javac`+JUnit-console harness against the actual production source (the new engine code was deliberately kept Bukkit-free per DESIGN_REVIEW.md's "isolate Bukkit types behind the Integration layer" specifically so that was possible) plus hand-review of the Bukkit-facing `knk-paper` half against this codebase's existing working call sites — not compiled, not run. The second session (the user's own machine) reached `repo.papermc.io` fine — a real `./gradlew build`/`test` passed clean on the first try (all 232 tests, both modules), confirming the hand-reviewed Bukkit API usage was correct. Live in-game testing then caught one real bug the build/tests couldn't have: `/knk menu page next\|prev` silently did nothing, because vanilla Minecraft closes/blocks the chat input while a custom Inventory screen has focus, so by the time the command actually ran, `MenuLifecycleListener`'s `InventoryCloseEvent` handler had already cleared the player's `OpenMenuContext` — `changePage()` required one to be present. Fixed: it now falls back to re-fetching the menu by the surviving `MenuSession.currentMenuKey()` and reopening fresh, and every silent no-op in that path now sends the player an actual message. Also fixed: the original `example.placeholder` seed's only section has one item under `HIDE` overflow, so pagination had nothing to demonstrate even with a correct section name — added `example.pagination` alongside it (not replacing it) with a `SCROLL` section holding 10 items across two pages. That section-name lookup was also made case-insensitive (`RuntimeMenu.findSection`, now covered by `RuntimeMenuTest`), since its only caller is a human typing a name into the debug command. **Confirmed live:** `/knk menu open example.pagination` then closing the GUI and running `/knk menu page next\|prev Content` visibly pages through the 10 items across both pages. Design calls made without a docs answer, flagged for confirmation rather than assumed silently: (1) `MenuOverflowMode.SCROLL` and `WRAP` are both treated as "paginate" at the base-class level (only `HIDE` truncates without paging) - the requirements doc's WRAP/SCROLL distinction is left to the Phase 7 preset-section-type library; (2) `MenuListMode` (DEFAULT/LINEAR/GRID) is parsed and carried through but Phase 2 fills every section with one uniform row-major order regardless of mode, for the same reason; (3) a `MenuItemTemplate` with no `materialRefId` falls back to `Material.PAPER` with a logged warning rather than failing the render. |
| InventoryMenu Phase 3 — variable resolution | plugin: Bukkit-free resolver/validator in `knk-core/.../core/menu/` (`VariableResolver`, `MenuVariableRefreshPolicy`, `MenuVariablePlaceholders`, `MenuDefinitionValidator`; `MenuSession` gained a per-binding-id resolved-value cache) plus the Bukkit-side wiring in `knk-paper/.../paper/menu/` (`MenuVariableContext`, `MenuDefinitionValidationRunner`, updated `MenuItemBukkitMapper`/`MenuRenderer`/`MenuService`), `KnKPlugin.onEnable` now runs startup validation, `/knk menu broken` added (`knk-plugin` branch `claude/inventory-menu-phase-2-x4y98c`, continued rather than forked since Phases 1-2 aren't merged to main yet); no web-api changes needed — Phase 1's schema already carried `RefreshPolicy`/`TtlTicks` | 2026-09-21 | `./gradlew clean build`: 622 tests, 0 failures, both modules. Phase 2's `MenuVariablePlaceholderText` stand-in is retired outright (deleted, not swapped in place) since real resolution needs a `MenuSession` and live context values (the actual `Player`) that its old `List<KnkVariableBinding> -> String` signature had no room for; its two call sites in `MenuItemBukkitMapper` now call `VariableResolver` directly. Reconciliation bug #7 (v2's broken `$(.*?)$` regex silently never substituted `%...%` variables, unnoticed for years) is fixed via one shared bounded pattern (`MenuVariablePlaceholders`, used by both the resolver and the validator so there's no second copy to drift) plus a required regression test (`VariableResolverTest.regressionBug7_realSubstitutionHappensEndToEnd`) asserting real end-to-end substitution, not just an architecture read-through. Caching is keyed by the binding's stable persisted id on `MenuSession`, not on the `KnkVariableBinding` instance — templates are reassembled fresh from the database on every menu open/page turn, so an instance-held cache would never survive past one render. `MenuDefinitionValidator` runs once per registered menu at plugin enable (not on the render path), resolving every getter-chain hop against a declared context type (`Player.class` for `$player.*$`, via `MenuVariableContext`) rather than a live instance; a menu that fails is logged loudly and blocked in `MenuService.blockMenu`, so every later open attempt refuses immediately with a clear reason instead of re-discovering the same failure per player. **Confirmed live:** `/knk menu open example.placeholder` — the item's name now reads the real player's username instead of the literal `$player.getName$` text (user-confirmed after a manual dev-server restart; this session couldn't drive a Minecraft client itself, only the build/tests/startup-log side). Open items flagged for Phase 4+, none blocking: Phase 2's three already-flagged design calls (SCROLL/WRAP both just paginate, `MenuListMode` uniform row-major, missing `materialRefId` falls back to PAPER) are still unresolved and untouched by this phase; the TTL default (20 ticks / 1s, per DESIGN_REVIEW.md §1) is unexercised by any real seed data yet, so it's unverified against real volatile-data content until something actually uses `RefreshPolicy=Ttl`. |
| Gate door snow-layer handling (item 7) | plugin: `knk-paper/.../gates/GateBlockPlacer.java` (`knk-plugin` branch `gate-structure-animation`); workspace: `docs/specs/gate-structure-animation/GATESTRUCTURE_QOL_IMPLEMENTATION_PLAN.md` | 2026-09-20 | Closed by explicit user decision **without** live verification: 7.1 implemented + unit-tested (`GateBlockPlacer.clearSnowLayerAbove`, `GateBlockPlacerTest`, 233/233 `knk-paper` green); 7.2's weather-dependent in-game retest on entity 14 was never done. If a hovering-snow report surfaces later, re-open with real evidence rather than assuming this already covers it. |
| Gate rotation gap-fill — rigid mid-swing animation (items 6.7/6.10/6.11, incl. 6.11.1-6.11.3) | plugin: `knk-core/.../gates/{RigidTransform,GateFrameCalculator,GateBlockPairing}.java`, `knk-core/.../domain/gates/CachedGateDoor.java`, `knk-paper/.../gates/{GateAnimationTask,GateLoaderAdapter,GateRestingFramePlacer}.java` (`knk-plugin` `17f016d`, branch `gate-structure-animation`); workspace: `docs/specs/gate-structure-animation/{ROTATION_GAP_FILL_DESIGN.md,GATESTRUCTURE_QOL_IMPLEMENTATION_PLAN.md}` | 2026-09-20 | Confirmed live (4 open/close cycles, entity 14, `GeometryDefinitionMode=REGION`): no jam, no orphaned blocks, rigid swing. See `GATESTRUCTURE_QOL_IMPLEMENTATION_PLAN.md` item 6.11.3 and `ROTATION_GAP_FILL_DESIGN.md` Decision 8.2 for full history. Item 7 (snow layer handling) is next. |
| Codebase scan — knk-web-api | knk-web-api (read-only); knk-workspace docs | 2026-09-18 | See `docs/reports/web-api-scan-2026-09-18.md`, `docs/architecture/web-api-architecture.md`, `docs/guides/web-api-reading-guide.md` |
| Codebase scan — knk-web-app | knk-web-app (read-only), knk-workspace (docs) | 2026-09-18 | See `docs/reports/web-app-scan-2026-09-18.md`, `docs/architecture/web-app-architecture.md`, `docs/guides/web-app-reading-guide.md` |
| InventoryMenu — engine reconciliation, design review, FormConfig scoping | knk-workspace (docs only) | 2026-09-21 | Reconciled Jan 2026 flexbox design against legacy bug findings; added search/filters/conditional-actions/permissions to spec; decided DB-persisted templates day one, FormConfig authoring UI deferred to a future update. See RECONCILIATION.md, DESIGN_REVIEW.md, FORMCONFIG_INTEGRATION.md. |
| InventoryMenu Phase 4 — permissions | plugin: `knk-core/.../core/menu/{RuntimeMenuItem,RuntimeMenuSection}.java` (added `isVisibleTo`/`isActionAllowedFor`, Bukkit-free `Predicate<String>`-based checks, plus `RuntimeMenuItemTest`/`RuntimeMenuSectionTest` coverage), `knk-paper/.../paper/menu/{MenuRenderer,MenuItemBukkitMapper,MenuClickListener}.java` (render-time section/item exclusion + click-time defense-in-depth) (`knk-plugin` branch `claude/inventory-menu-phase-2-x4y98c`, continued); web-api: `Models/Menu/MenuTemplateSeed.cs` (additive `example.permissions` seed: a "Debug Tools" section literally recreating reconciliation bug #10's ungated Caches button behind a whole-section `VisibilityPermission`, plus a "Preview" item demonstrating an independent `ActionPermission`-only gate) | 2026-09-21 | Closed by explicit user decision **without** live verification: `./gradlew build`/`test` green (397 knk-core + 233 knk-paper tests, 0 failures, includes new permission-check unit tests), `dotnet build` on knk-web-api 0 errors (verified via a scratch output dir since the live dev API instance's DLL was locked by an attached debugger — that process was left untouched). The dev-server plugin jar was rebuilt/deployed via `deployToDevServer`, but the user was actively connected in-game at the time and chose to restart and live-test themselves rather than have this session do it. **Not yet confirmed live:** that a permission-gated item/section is actually hidden/disabled in a real client for a player without the node, and becomes visible/actionable once granted (`/op`/`/deop __pandi__` toggles it — no permissions plugin is installed on this dev server, so the two demo permission nodes are deliberately left unregistered in `plugin.yml` to use Bukkit's own unregistered-permission-defaults-to-op fallback). If a permission-gating bug is reported later, re-verify with real evidence rather than assuming this already covers it. Open items carried forward, none blocking: Phase 2/3's already-flagged design calls (SCROLL/WRAP both just paginate, `MenuListMode` uniform row-major, missing `materialRefId` falls back to PAPER, TTL default unexercised) remain untouched and are still Phase 7's concern. |
| InventoryMenu Phase 5 — search & filters | plugin: `knk-core/.../core/menu/{MenuContentQuery.java (new), MenuSession, RuntimeMenuSection, VariableResolver, MenuTemplateAssembler}.java`, `knk-core/.../core/domain/menu/KnkMenuSectionTemplate.java` (added `searchable`); `knk-paper/.../paper/menu/{MenuRenderer,MenuService}.java`, `knk-paper/.../paper/chat/{ChatCaptureManager,CaptureFlow,CaptureStep}.java` (new generic `TEXT_INPUT` flow), `knk-paper/.../paper/commands/{MenuDebugCommand,KnkAdminCommand}.java` (`/knk menu search\|filter` dev-harness subcommands), `KnKPlugin.java` (wires `chatCaptureManager` into `MenuService`) — all on `knk-plugin` branch `claude/inventory-menu-phase-2-x4y98c`, continued; web-api: `Models/Menu/MenuSectionTemplate.cs` (additive `Searchable` bool column + migration `20260922120000_AddMenuSectionSearchable`), `Dtos/MenuDtos.cs`, `Services/MenuTemplateService.cs`, `Models/Menu/MenuTemplateSeed.cs` (additive `example.search` seed: an "Instructions" static-text section + a 2×2 SCROLL `Content` section with 8 fruit/berry items spanning 2 pages, each carrying a `Category` `VariableBinding` for the filter demo) — same web-api branch `claude/inventory-menu-phase-2-x4y98c` | 2026-09-22 | **Branch note (read this before starting Phase 6):** this session's harness assigned a fresh branch (`claude/inventory-menu-phase-5-4oqu9v`) forked from `main`/`master` in `knk-web-api` and `knk-plugin` — it had *none* of Phases 1-4's commits. Confirmed via `git log`/`git merge-base` that all that work still lives only on the unmerged `claude/inventory-menu-phase-2-x4y98c` in both repos. Per explicit user decision (asked directly rather than guessing), this session switched both repos' working branch to `claude/inventory-menu-phase-2-x4y98c` and pushed there, continuing the established pattern — **if Phase 6 is handed a fresh `phase-6-*` branch again, check for this same mismatch before writing any code.** `knk-web-app` and `knk-workspace` were unaffected (no prior-phase work in web-app; workspace's assigned branch already matched `main`).  **Open questions resolved (see task instructions for the full text of each):** (1) *What does search/filter narrow?* A predicate applied over `RuntimeMenuSection.items()` post-assembly, not a live catalog query — Phase 8's job, untouched here. (2) *Resolved or raw text?* Resolved — the new `RuntimeMenuSection.resolveSlots(int,int,Predicate<RuntimeMenuItem>)` overload takes an opaque `Predicate<RuntimeMenuItem>` (knk-core stays Bukkit-free, no resolution logic there); the *matching* itself happens in knk-paper's `MenuRenderer.buildContentFilter`, which resolves every candidate item's `Name` binding (and any active filter facet's binding) via `VariableResolver` + the live `Player` context **before** calling `resolveSlots`, so a section's *entire* item list gets resolved on a search/filter-active render pass, not just the current page's slice — a real perf cost accepted for correctness at this phase's scope, worth profiling if a section ever grows very large (Phase 8 territory). (3) *Text input mechanism:* confirmed and reused `ChatCaptureManager` rather than building an anvil GUI — added a new generic `CaptureFlow.TEXT_INPUT`/`CaptureStep.TEXT_INPUT` + `ChatCaptureManager.startTextCapture(player, prompt, onComplete, onCancel)` (no fixed prompt/multi-step state, unlike the existing `ACCOUNT_MERGE` flow). Confirmed this reuses the *exact* same GUI-closes-when-chat-opens constraint Phase 2 already found for `/knk menu page`: a player must close the menu Inventory to type a chat-captured query, so `MenuService.promptSearch`/`promptFilter`'s completion callback re-fetches the `MenuSession` by its surviving `currentMenuKey()` and reopens fresh (`applyContentQueryUpdate`), exactly like `changePage`'s established fallback — not a new problem, the same one. (4) *FilterBar structured facets:* did **not** add `Category`/`Grade`/`Tag` columns to `MenuItemTemplate` (out of this task's authority per the standing instructions — would be a web-api schema/migration decision). Instead, `MenuContentQuery.filterValues` is a generic `Map<targetProperty, value>` matched via a new `VariableResolver.resolveByTargetProperty(bindings, targetProperty, ...)` against the *already-generic, already-persisted* `VariableBinding.targetProperty` field — a content author attaches e.g. a `Category`-targeted `VariableBinding` to an item with zero schema change, demonstrated by the `example.search` seed's `Category` bindings. Ready to re-point at real catalog fields whenever Phase 8 lands them.  **One additional design call, not in the original open questions, flagged here rather than assumed:** the plan text specifies one `searchable` flag (not a separate `filterable` flag). Interpreted `searchable` as gating *both* search-text and filter-facet predicate application for a section — i.e. "this section participates in the active-content-predicate mechanism at all" — since DESIGN_REVIEW.md §2.3 says both compose through one shared mechanism. If that reading turns out wrong, it's a one-word rename plus re-threading one boolean, not a redesign.  **Mechanism:** `MenuSession.setContentQuery`/`getContentQuery` (keyed by section template id, same convention as `sectionPages`) holds the per-section `MenuContentQuery` (search text + filter facet map), and explicitly calls `markDirty()` — DESIGN_REVIEW.md §1 names "a filter/search updated" as an explicit `ON_DIRTY` trigger, so this is the one place that transition happens. `RuntimeMenuSection.resolveSlots`'s new overload applies the content filter only to auto-placed items, never to pinned ones (`slotOverride` set) — a persistent search/clear button must not disappear because of its own search. Empty-results state: `MenuRenderer` places a Barrier "No results found" item into the first free slot in the section's own layout footprint when a query is active but matched zero auto items, skipped silently if the whole footprint is pinned.  **Trigger surface (Phase 5 scope, not Phase 7's polished SearchBar/FilterBar presets):** `/knk menu search <sectionName> [clear]` and `/knk menu filter <sectionName> <facetKey> [clear]` — same dev-harness-command relationship Phase 2 already established for `/knk menu page next\|prev`, standing in for the real clickable button Phase 7's preset renderer will eventually provide. `MenuSectionKind.SEARCH_BAR`/`FILTER_BAR` still carry no renderer-dispatch behavior — unchanged, still Phase 7's job.  **Verified:** `knk-core`'s entire `menu` package (main + test, including every new/changed file) compiles and passes standalone via `javac` + a downloaded `junit-platform-console-standalone` jar — **272/272 tests green, 0 failures** — since this session's sandbox has `repo.papermc.io` blocked (403, confirmed via the proxy's own status endpoint, same finding as Phase 1/2's cloud sessions) so `./gradlew` can't resolve `paper-api` for *any* module in this Gradle project, even Bukkit-free `knk-core`, because Gradle resolves per-module not per-package. Maven Central *is* reachable, so the touched `knk-api-client` DTO/mapper files (`MenuSectionTemplateDto`, `MenuTemplateMapper`, and their transitive dto siblings) were also standalone-compiled clean against the built `knk-core` output + a fetched `jackson-annotations` jar. **Not verified by this session, unlike some earlier ones:** the Bukkit-dependent `knk-paper` half (`MenuRenderer`, `MenuService`, `ChatCaptureManager`, `MenuDebugCommand`, `KnkAdminCommand`, `KnKPlugin.java`) — no paper-api jar was reachable or already cached anywhere in this sandbox (checked; none found), and no dev server was reachable either (`gradle.properties`' `devServerDirectory` points at a Windows path on the user's own machine, not present here). This is hand-reviewed carefully against every existing call-site precedent (method names/signatures cross-checked line-by-line against the now-compiling knk-core API) but **not compiler-verified**. `knk-web-api`: no `dotnet`/`dotnet-ef` SDK is installed in this sandbox at all (checked — not a locked-file issue like Phase 4's, the SDK itself isn't present), so the new EF Core migration (`20260922120000_AddMenuSectionSearchable.cs` + hand-authored `.Designer.cs`, copied from the prior migration's snapshot and diffed by the one new property, plus the matching `KnKDbContextModelSnapshot.cs` edit) is **not build- or apply-verified** — written by hand following the exact pattern of the most recent single-column migration (`AddGateStructureIsJammed`), but needs a real `dotnet build` + `dotnet ef database update` against a real MySQL instance before trusting it, the same way Phase 1 first proved its migration end-to-end.  **Needed before Phase 6, in order of priority:** (1) a real `./gradlew build`/`test` on the user's own machine (where `repo.papermc.io` resolves) to confirm the `knk-paper` changes actually compile — this is the biggest real risk carried forward, bigger than usual since it's untouched by any compiler this time; (2) `dotnet build` + `dotnet ef database update` for the new migration; (3) live verification once both build clean: open `example.search`, confirm `/knk menu search Content <query>` narrows the 8-item catalog and still pages correctly, confirm `/knk menu filter Content Category Berry` narrows to the 3 berry items, confirm a query with zero matches shows the "No results found" barrier instead of a blank section, and confirm `clear` restores the full list. Phase 4's live-verification gap (permission gating) was **not** folded into this pass — this session never reached a dev server at all, so there was no opportunity to fold anything in; that gap is still exactly as open as Phase 4 left it. Design calls carried forward from Phase 2/3 (SCROLL/WRAP both just paginate, `MenuListMode` uniform row-major, missing `materialRefId` falls back to PAPER, TTL default unexercised) remain untouched and still Phase 7's concern. **Post-completion note (2026-09-22):** the developer flagged, after this phase closed, that they don't want `/knk menu ...` commands to remain the main/only way to trigger pagination or search/filter — they're a dev harness, not the intended player-facing UX. A same-day research pass (a different session) then proposed an interaction model: UI-first/command-fallback, click-driven pagination/filtering with in-place re-render, AnvilGUI (md5lukas fork) for free-text input, click actions (+1/-1/shift-click) for numeric input with anvil as fallback, in-menu confirm/cancel items, and InvUI as the chest-menu construction/pagination library. The developer then explicitly refined this same day: build both patterns **in-house**, not as third-party dependencies — an InvUI-*inspired* clickable-item/pagination model extending Phase 2's already-shipped in-house rendering engine, and an AnvilGUI-*inspired* small self-contained anvil-capture component, both built directly against the Bukkit API, no new jars. This **explicitly supersedes** Phase 5's `ChatCaptureManager`-based search input (open question 3's resolution above) — that mechanism was a reasonable call at the time but is now the wrong one per this later decision, not an option kept alongside the new in-house anvil capture. Full decision recorded in `docs/specs/inventory-menu/DESIGN_REVIEW.md` §2.1 (updated) and new §2.5; tracked for implementation in `docs/backlog/QOL_BUGFIX_BACKLOG.md` item 8, which also flags a real open question this research didn't cover: how much of the InvUI-inspired behavior is genuinely new work on top of Phase 2's already-shipped, deliberately modular engine (`MenuSlotCalculator`/`MenuRenderer`/`MenuItemBukkitMapper`) versus already covered — read those classes first rather than assume a rewrite is needed. This still doesn't reopen Phase 5 itself (the underlying predicate/session mechanism is unaffected either way, only the trigger/input surface changes), but whoever implements Phase 6/7 (or a pulled-forward interim phase — sequencing is also still open, see backlog item 8) needs to build against this in-house decision, not Phase 5's chat-capture approach and not a third-party library. |
| InventoryMenu Phase 6 — conditional actions | plugin: `knk-core/.../core/menu/{ActionRegistry,ConditionRegistry,ConditionOutcome,MenuActionException,MenuParams}.java` (new), `MenuDefinitionValidator.java` (added `validateActionsAndConditions`), plus `ActionRegistryTest`/`ConditionRegistryTest`/`MenuParamsTest`/`MenuDefinitionValidatorTest` coverage; `knk-paper/.../paper/menu/{MenuActionContext,MenuActionHandlers,MenuConditionHandlers}.java` (new), `MenuClickListener.java` (actual click-time execution, replacing Phase 2's "execution deferred to Phase 6" stand-in), `MenuDefinitionValidationRunner.java` (calls the new registry-id validation), `KnKPlugin.java` (wires the registries + default handlers at plugin enable) — all `knk-plugin` branch `claude/inventory-menu-phase-2-x4y98c`, continued; web-api: `Models/Menu/MenuTemplateSeed.cs` (additive `example.conditions` seed demonstrating item-level vs. action-level condition composition) — same web-api branch | 2026-09-22 | **Branch note:** this session's harness again assigned fresh branches (`claude/inventory-menu-phase-6-584klt`) forked from `main` in `knk-plugin` and `knk-web-api`, missing Phases 1-5's commits — the exact pattern Phase 5's entry above documents. Confirmed via `git log` on both, switched both repos to `claude/inventory-menu-phase-2-x4y98c` and pushed there without asking, per that entry's explicit precedent (the task instructions also named this as an established pattern, not a new judgment call). `knk-workspace` was unaffected (assigned branch already matched `main`); `knk-web-app` has no InventoryMenu work and wasn't touched.  **Open questions resolved (see task instructions for the full text of each):** (1) *Initial registry contents.* `MenuVariableContext` still only declares `player` (no economy/ownership/item-age concept anywhere in the live context), so DESIGN_REVIEW.md §2.2's suggested affordability/ownership/item-age conditions are **not implemented** - building them now would mean inventing fake data to back them, the same call Phase 5 made for missing `Category`/`Grade`/`Tag` schema. What's real and implemented: actions `menu.close` (`Player.closeInventory()`) and `menu.open` (`{"key": "..."}` param, delegates to `MenuService.openMenu`); conditions `always` (trivial baseline, unblocks the existing seed) and `permission-node` (`{"node": "..."}` param, `Player::hasPermission`). (2) *Registry split.* `ActionRegistry<C>`/`ConditionRegistry<C>` (generic over the execution context type) live in knk-core - just a String-keyed map, lookup, and "unregistered id throws `MenuActionException`" failure mode, provably Bukkit-free since `ActionRegistryTest`/`ConditionRegistryTest` exercise them with a bare `String` as `C`. knk-paper defines the concrete `MenuActionContext` record (`Player`, `MenuSession`, live variable-context `Map`, `MenuService`) and instantiates `ActionRegistry<MenuActionContext>`/`ConditionRegistry<MenuActionContext>`, registering the real handlers in new `MenuActionHandlers`/`MenuConditionHandlers` classes at plugin enable - directly mirrors the `MenuVariableContext.DECLARED_TYPES` (knk-core-visible shape) vs. `liveValues` (knk-paper-only live values) split already established for variable resolution. This is a deliberate deviation from IMPLEMENTATION_PLAN.md's literal "`Predicate<MenuSession>` factory" wording: `MenuSession` alone has no `Player` reference (by design - it's Bukkit-free), so a bare `Predicate<MenuSession>` can't check a permission node; the richer generic context was necessary, not a scope-creep choice. (3) *Item-level vs. action-level composition.* A failing item-level condition (`ActionBindingId` null) aborts every action on the item - no partial execution. Each action's own conditions (`ActionBindingId` set) then gate just that one action independently, per `KnkConditionBinding`'s own existing javadoc ("gates just that one action") - one action on an item can fire while a sibling action on the very same click doesn't. Covered by `example.conditions`' two demo items and is exactly the kind of regression a click-flow refactor could silently invert, so it's also worth a `MenuClickListener` unit test once that class gets one (see below - none exists yet for this class in any phase). (4) *Player-facing feedback.* `ConditionRegistry`'s handler returns a new `ConditionOutcome(allowed, denialMessage)` record instead of a bare boolean, so a condition type opts into a player-facing message on denial. Decision: **conditions get feedback, unlike Phase 4's `actionPermission` check**, which stays exactly as silent as before - `actionPermission` is a structural authorization boundary (Phase 4 deliberately never reveals which node is missing), whereas a Condition is a declarative business-rule gate a content author attaches on purpose, and DESIGN_REVIEW.md §2.2's own example ("you can't afford this") is explicitly meant to be player-visible. `permission-node`'s denial message is a generic "You don't have permission to do that." (reveals denial, not which node would grant it) - every condition type this phase ships follows the same non-revealing-but-visible rule. (5) *Staleness/re-check.* `MenuClickListener` builds a brand new `MenuActionContext` on every click via `MenuVariableContext.liveValues(player)` - never anything cached from the render pass, and there is no condition-result cache anywhere in the click path (unlike `VariableResolver`'s deliberate `ON_DIRTY`/TTL render-time cache) - so a permission granted/revoked between menu-open and click is reflected immediately by construction, not by an added invalidation step. `example.conditions`' "Item-level gate demo" is built specifically to demonstrate this live (see below).  **Load-time validation:** confirmed `MenuDefinitionValidator` had no action/condition-id check before this phase (only variable-binding validation existed) - added `validateActionsAndConditions(RuntimeMenu, Set<String> registeredActionTypeIds, Set<String> registeredConditionTypeIds)`, called from `MenuDefinitionValidationRunner.runAtStartup` (now takes the two registries' `registeredIds()` snapshots) right after the existing variable-binding `validate` call, so a menu referencing an unregistered id is blocked at plugin enable with every broken reference listed, not discovered per-click.  **Verified:** `knk-core`'s entire `menu` package (main + test, all new/changed files) compiles and passes standalone via `javac` + `junit-platform-console-standalone` against Maven Central jars - **286/286 tests green, 0 failures** (up from Phase 5's 272; the 14 new tests are `ActionRegistryTest`/`ConditionRegistryTest`/`MenuParamsTest` plus 4 new `MenuDefinitionValidatorTest` cases). `repo.papermc.io` is blocked in this sandbox (confirmed both via a direct `curl` 403/connect-rejected and by `./gradlew :knk-core:test` itself failing before even reaching per-module resolution, at the `knk-paper` module's `com.gradleup.shadow` plugin-portal lookup) - the same finding as every prior InventoryMenu phase run from a cloud sandbox. **Not compiler-verified:** the Bukkit-dependent `knk-paper` half (`MenuActionContext`, `MenuActionHandlers`, `MenuConditionHandlers`, `MenuClickListener`, `MenuDefinitionValidationRunner`, `KnKPlugin.java`) - no `paper-api` jar reachable or cached anywhere in this sandbox (checked). Hand-reviewed line-by-line against every real call site (`MenuSessionRegistry.get`, `ActionRegistry.execute`/`ConditionRegistry.test` signatures, `KnkActionBinding`/`KnkConditionBinding` accessor names, `MenuVariableContext.liveValues`) - every constructor/method call in the new and edited knk-paper files was cross-checked against the actual declared signature it calls, not assumed. `knk-web-api`: no `dotnet`/`dotnet-ef` SDK is installed in this sandbox at all (checked, same finding as Phase 5) - the additive `example.conditions` seed is hand-reviewed against the real `MenuItemTemplate`/`ActionBinding`/`ConditionBinding` model classes and `Enums/MenuEnums.cs` (every enum value and property name used in the new seed cross-checked against the actual C# source), but not build-verified.  **Not done, no live Minecraft client or dev server was reachable from this sandbox at all** (unlike Phase 4/5's later stages) - none of the "click closes the menu", "a designed-to-fail condition blocks its action and shows feedback", or "a condition re-evaluated at click time reflects a mid-session permission change" scenarios from the task's verification checklist were exercised against a running server. This is a materially bigger verification gap than Phase 4/5 left (they at least got a real `./gradlew build`/`dotnet build` from a second session with better network access) - **strongly recommend a follow-up pass on a machine that can reach `repo.papermc.io` and has the `dotnet` SDK, both compiling clean and then live-testing `example.conditions` end-to-end** (open the menu as a non-op player: clicking "Item-level gate demo" should show the denial message and leave the menu open; `/op` the player without reopening the menu, click again: the item should now close the menu, proving the live re-check; "Action-level gate demo" should always close the menu while additionally showing the denial message when the player lacks the action-level node) before trusting this phase the way Phase 4/5 were eventually trusted.  **Open items carried forward, none blocking Phase 7:** Phase 2/3's design calls (SCROLL/WRAP both just paginate, `MenuListMode` uniform row-major, missing `materialRefId` falls back to PAPER, TTL default unexercised) remain untouched; Phase 4's permission-gating live-verification gap and Phase 5's build+live-verification gap are both still exactly as open as those phases left them - this session never reached a dev server either, so nothing could be folded in. `MenuSectionKind.SEARCH_BAR`/`FILTER_BAR`/preset section-type rendering, and any clickable Phase 7 SearchBar/FilterBar button that would call into this phase's new action-execution path, remain entirely unbuilt - this phase only makes click → condition-check → action-execution work in general, per IMPLEMENTATION_PLAN.md's explicit phasing. |
| InventoryMenu Phase 7 — preset section/item library (+ QOL_BUGFIX_BACKLOG.md item 8 folded in) | plugin: `knk-core/.../core/menu/MenuSession.java` (added `PendingConfirmation` record + get/set/clear, plus `MenuSessionPendingConfirmationTest`); `knk-paper/.../paper/menu/{MenuActionContext,MenuRenderResult,OpenMenuContext,MenuRenderer,MenuService,MenuClickListener,MenuActionHandlers,MenuConditionHandlers,KnKPlugin}.java` (extended/wired), `AnvilCaptureManager.java` (new, in-house anvil-capture component), `MenuDebugCommand.java` (doc-comment update only) — all `knk-plugin` branch `claude/inventory-menu-phase-2-x4y98c`, continued; web-api: `Models/Menu/MenuTemplateSeed.cs` (additive `example.presets` seed demonstrating every new preset end-to-end) — same web-api branch; workspace: this entry + `docs/backlog/QOL_BUGFIX_BACKLOG.md` item 8 (resolved questions 5-6, status → Implemented) | 2026-09-22 | **Branch note:** this session's harness again assigned fresh branches (`claude/inventory-menu-phase-7-jjmjcq`) forked from `main`/current trunk in `knk-plugin` and `knk-web-api`, missing Phases 1-6's commits — the exact repeated pattern Phase 5/6's entries document. Confirmed via `git log` on both (their assigned branches carried only unrelated gate-animation commits), switched both repos to `claude/inventory-menu-phase-2-x4y98c` and pushed there without asking, per that established precedent. `knk-workspace` was committed straight to `main` as instructed; `knk-web-app` has no InventoryMenu work and wasn't touched.

**Open questions resolved (see task instructions for the full text of each):**

(1) *SectionTypeRegistry: build it, or not?* **Not built.** `MenuSectionKind` stays a plain 5-value enum with zero registry/dispatch machinery — no `SectionTypeRegistry` class exists. Reasoning: once open question 2 (below) settled on hand-authored preset buttons rather than kind-driven auto-rendering, there turned out to be *nothing left* for a per-kind renderer to dispatch to — every section, regardless of `kind`, still renders through the exact same Phase 2 pipeline (`RuntimeMenuSection.resolveSlots` → `MenuRenderer.computeState`) it always has; `ConfirmDialog`/`SearchBar`/`FilterBar` are just `StaticButtons`-shaped sections whose items happen to carry the new `menu.confirm.*`/`menu.search.*`/`menu.filter.*` action ids. Building a String-keyed registry with nothing distinct to route to would be exactly the "general registry/dispatch machinery a fixed, small problem doesn't need" DESIGN_REVIEW.md's own flexbox-tempering argument warns against. `MenuSectionKind` remains a descriptive/organizational label an author sets for their own clarity and for future phases (e.g. Phase 8 catalog paging) to key off if they ever need to — Phase 7 itself never branches on it anywhere.

(2) *Auto-placement vs. hand-authored preset buttons?* **Hand-authored `MenuItemTemplate` rows** (matching every existing seed's convention — a content author places a `SlotOverride`d item with a `Name` binding and wires an `ActionBinding`), **not** kind-driven auto-injection of trigger items. A `kind=SearchBar` section does *not* automatically grow a search button. However, per the task's own framing of this as a real design tension (a "preset in name only" if a content author has to hand-wire everything): the compromise is that the new action handlers derive their *target section* from click context, not from a hand-typed `paramsJson` section name — see open question below and `MenuActionContext`'s javadoc. So a content author still places the button once (slot, material, name — exactly like any other button), but never types a section name/id into params by hand, and the exact same button definition works verbatim in any section without editing params per-instance. This is a real, if partial, answer to "should work without hand-wiring section names into params" — full zero-authoring presets (a kind that renders its own controls) was rejected as more machinery than a 5-value enum and this phase's actual seed-authoring burden justify.

(3) *MenuActionContext's known gap: how does a section-scoped action know its section?* Resolved by extending `MenuActionContext` with `RuntimeMenu menu` and `RuntimeMenuSection section` (the clicked item's containing section) — exactly the "extending MenuActionContext" option the task flagged as "almost certainly correct for a preset component." Plumbed through: `MenuRenderResult` gained a third map (`sectionsBySlot`, built in the same per-section loop `MenuRenderer.computeState` already runs), `OpenMenuContext` carries it alongside the existing `itemsBySlot` snapshot, and `MenuClickListener` looks up the clicked slot's section from that same snapshot before constructing the (still freshly-built-per-click, per Phase 6's staleness fix) `MenuActionContext`. `section` is effectively never null for a real click (every rendered item belongs to exactly one section), but every handler that needs it still checks defensively via a `requireSection` helper that throws `MenuActionException` rather than assume, since nothing prevents a future non-click caller from constructing a context without one.

(4) *In-house anvil-capture component.* Built as `AnvilCaptureManager` (`knk-paper/.../paper/menu/AnvilCaptureManager.java`), mirroring `ChatCaptureManager.startTextCapture(player, promptMessage, onComplete, onCancel)`'s exact signature for a clean swap. Opens a virtual `AnvilInventory` (`Bukkit.createInventory(null, InventoryType.ANVIL, "Enter text")` — the plain-String title overload, not the Adventure `Component` one, to avoid any doubt about which `createInventory` overloads exist without being able to compile against real Paper API this session), puts a `Material.PAPER` item in slot 0 so the client shows the rename field, lets vanilla Minecraft compute the renamed result in the output slot (slot 2 / `SlotType.RESULT`) for free (a plain rename with no second input item is always a valid, low-cost anvil operation), reads that slot's display name on a click there, cancels every other click in the anvil, and defers both the `player.closeInventory()` call and the `onComplete` callback to the next tick (avoiding same-tick reentrancy from closing an `Inventory` inside its own click handler). `PrepareAnvilEvent` zeroes the repair cost on every prepare so a rename never costs XP or hits the "Too Expensive!" cap. Scope kept deliberately narrow per the task: only `MenuService.promptSearch`/`promptFilter` were switched from `chatCaptureManager.startTextCapture` to `anvilCaptureManager.startTextCapture` (constructor signature changed accordingly, wired at `KnKPlugin.onEnable`); `ChatCaptureManager` itself, its `ACCOUNT_MERGE` flow, and its generic `TEXT_INPUT`/`CaptureStep` mechanism are all completely untouched and still exist (now simply uncalled by InventoryMenu, not deleted — no instruction to rip out reusable infra another flow could still use).

(5) *Confirm dialog mechanism.* New `MenuSession.PendingConfirmation(actionTypeId, actionParams, promptMessage)` record + `getPendingConfirmation`/`setPendingConfirmation`/`clearPendingConfirmation` on the existing Bukkit-free `MenuSession` (one pending confirmation per session, not per-item — requesting a new one simply overwrites). Three new actions: `menu.confirm.request` (params `actionTypeId`, optional `actionParamsJson`/`prompt`) stores the pending confirmation and messages the player — it never runs the wrapped action itself; `menu.confirm.accept` reads the pending confirmation, clears it, and re-invokes it via `ActionRegistry.execute` using the *same registry instance* the accept handler is registered on (captured as a closure in `MenuActionHandlers.registerDefaults`, simpler than threading the registry itself through `MenuActionContext` for the one action that needs it); `menu.confirm.cancel` clears it and messages the player. Both accept/cancel throw `MenuActionException` if nothing is pending — normally unreachable because the new `has-pending-confirmation` condition (added to `MenuConditionHandlers`) is expected on both Confirm/Cancel buttons, giving a clean player-visible "Nothing to confirm." denial instead. `example.presets`' "Confirm Demo" section demonstrates the whole loop: "Do Something Risky" only requests confirmation for `menu.close`; only clicking Confirm actually closes the menu, Cancel discards the request, and clicking either first (nothing pending yet) shows the condition's denial message.

(6) *Numeric input.* **Confirmed not built, per the task's own recommended call** — no quantity-bearing content exists anywhere in InventoryMenu (no shop/kit screen), so building +1/-1/shift-click/anvil-fallback quantity selection now would mean inventing fake content to demonstrate against, the same trap Phase 5 explicitly avoided for missing `Category`/`Grade`/`Tag` schema and Phase 6 explicitly avoided for missing economy/ownership context. Flagged, not silently dropped — still named in DESIGN_REVIEW.md §2.5's interaction model and explicitly out of this phase's scope per the task text.

(7) *Same underlying MenuService methods for clicks and commands.* Confirmed by construction, not just intent: every new section-scoped action handler (`menu.page.next/prev`, `menu.search.prompt/clear`, `menu.filter.prompt/cycle/clear`) calls the exact same `MenuService.nextPage`/`previousPage`/`promptSearch`/`clearSearch`/`promptFilter`/`filter`/`clearFilter` methods `MenuDebugCommand`'s `/knk menu page|search|filter` subcommands already called before this phase — zero duplicated pagination/search/filter logic, exactly per QOL_BUGFIX_BACKLOG.md item 8's open question 6.

**QOL_BUGFIX_BACKLOG.md item 8's own remaining open questions (5, 6) resolved** as part of this phase — see that item's updated "Resolved questions" section for the full text; in short, question 5 (how much was genuinely new engine work) resolved to "none — zero new rendering/pagination/layout logic, only the slot→section routing plumbing," and question 6 (fold into Phase 7 or its own phase) resolved to "folded into Phase 7," per the developer's 2026-09-22 decision named directly in this session's task instructions.

**`example.presets` seed** (web-api, additive — does not touch `example.pagination`/`example.search`/`example.conditions`): a 4-row (36-slot) menu. Row 0-1: a 5×2 `Scroll`/`Grid` `Searchable` "Content" section with 16 Fruit/Berry catalog items (capacity 10 → 2 pages, same category split as `example.search` but doubled so pagination is actually observable) plus six pinned (`SlotOverride`) control buttons in the section's own unused columns — Previous/Next (`menu.page.prev`/`.next`), Search/Clear Search (`menu.search.prompt`/`.clear`), Filter-cycle/Clear Filter (`menu.filter.cycle` with `values="Fruit,Berry"` / `menu.filter.clear`). Row 2: "Confirm Demo" (`ConfirmDialog` kind) — "Do Something Risky" (`menu.confirm.request` wrapping `menu.close`), "Confirm" and "Cancel" (both gated by `has-pending-confirmation`). Row 3: a static instructions line. Every pinned button is unaffected by its own section's active search/filter (same pinned-item guarantee `example.search`'s persistent controls already rely on since Phase 5).

**Verified:** `knk-core`'s entire `menu` package (main + test, every new/changed file) compiles and passes standalone via `javac` + `junit-platform-console-standalone` against Maven Central jars — **291/291 tests green, 0 failures** (up from Phase 6's 286; the 5 new tests are `MenuSessionPendingConfirmationTest`). This session's sandbox has `repo.papermc.io` explicitly policy-denied (confirmed via a direct `curl` 403 *and* the proxy's own `/__agentproxy/status` endpoint listing two `connect_rejected` entries for that host at the time of this session) — same finding as every prior InventoryMenu phase run from a cloud sandbox; the Gradle plugin portal itself (`plugins.gradle.org`) *is* reachable this session (200), but `./gradlew` still can't get past configuring the `knk-paper` module's `paper-api` dependency (and, newly confirmed this session, `knk-core` itself also directly depends on `compileOnly`/`testImplementation("io.papermc.paper:paper-api:...")` for an unrelated `Vector`-using test helper, so even a `knk-core`-only Gradle task fails at the same wall — this is why every prior phase's standalone-`javac` fallback approach is necessary, not just a `knk-paper`-only workaround). **Not compiler-verified:** the Bukkit-dependent `knk-paper` half (`AnvilCaptureManager` and every other changed file in `knk-paper/.../paper/menu/`, plus `KnKPlugin.java`) — no `paper-api` jar reachable or cached anywhere in this sandbox (checked). Hand-reviewed line-by-line against every real call-site precedent in this codebase (`ActionRegistry.execute`/`ConditionRegistry.test` signatures, `MenuSession`/`RuntimeMenuSection`/`RuntimeMenuItem` accessor names, `MenuService`'s existing public method signatures) for everything with a precedent; `AnvilCaptureManager` is inherently new API surface with **no existing call site in this codebase to cross-check against** (grepped for prior `InventoryType`/`AnvilInventory`/`PrepareAnvilEvent`/`SlotType` usage anywhere in `knk-paper` — none found), so its Bukkit API usage (`InventoryType.ANVIL`, `AnvilInventory.setRepairCost`, `PrepareAnvilEvent`, `InventoryType.SlotType.RESULT`) rests on general Bukkit/Paper API knowledge rather than an in-repo precedent — flagged as the single highest-risk file in this phase for a first live test to actually exercise. `knk-web-api`: no `dotnet`/`dotnet-ef` SDK is installed in this sandbox at all (checked, same finding as every prior phase) — `example.presets` is hand-reviewed against the real `MenuItemTemplate`/`MenuSectionTemplate`/`ActionBinding`/`ConditionBinding`/`VariableBinding` model classes and `Enums/MenuEnums.cs` (every property name and enum value cross-checked against the actual C# source), and involves no schema/migration change at all (confirmed per the task's own expectation — actions/conditions still resolve through Phase 1's `actionTypeId`/`conditionTypeId`/`paramsJson`, unchanged), but is not build-verified.

**Not done — no live Minecraft client or dev server was reachable from this sandbox at all** (same as Phase 6, worse than Phase 4/5's later stages): none of this phase's own verification checklist (Next/Previous clicking alone, Search opening an anvil and narrowing results, Filter-cycle narrowing by category, Confirm/Cancel actually gating `menu.close`, `/knk menu ...` commands still working) was exercised against a running server, and Phase 6's still-never-live-verified `example.conditions` click flow could not be folded in either, for the same reason (no dev server, no Minecraft client, ever reachable this session). This is now **three consecutive phases (5, 6, 7) whose knk-paper changes have never been compiler-verified, let alone live-tested**, on top of Phase 4's still-open permission-gating live-verification gap — a materially larger accumulated risk than any single phase's own entry can convey. **Strongly recommend, before Phase 8 starts:** a real `./gradlew build`/`test` on a machine that can reach `repo.papermc.io` (this would be the first time Phases 5-7's combined `knk-paper` changes compile at all), then a live pass covering, in order: (a) Phase 4's original permission-gating gap, (b) Phase 6's `example.conditions` click flow (item-level vs. action-level denial, live permission-change staleness fix), (c) Phase 7's own checklist above via `example.presets`, with `AnvilCaptureManager` getting the closest scrutiny as the file with no in-repo precedent. If any of (a)/(b)/(c) reveals a real bug, fix it before layering Phase 8's catalog-paging work on top of an unverified foundation.

**Design calls carried forward, none blocking Phase 8:** Phase 2/3's original three (`SCROLL`/`WRAP` both just paginate, `MenuListMode` uniform row-major, missing `materialRefId` falls back to `PAPER`) remain untouched and are no longer "Phase 7's concern" as prior entries deferred them to be — Phase 7 deliberately did not revisit menu layout/list-mode mechanics at all, so these are now open for whoever eventually needs `WRAP`'s distinct behavior or a non-row-major `MenuListMode`, with no phase currently claiming them. Pagination-arrow items do not visually disable/hide at the first/last page boundary (clicking "Next" on the last page is a safe, harmless no-op via `MenuSession.nextPage`'s existing clamping, just not visually indicated) — deliberately not built, since it would need new item-level boundary-awareness/dynamic-lore machinery (`SectionSlotAssignment.hasNext`/`hasPrev` exist but nothing consumes them yet) beyond what "real, clickable" pagination requires per this phase's actual scope. |
| Items — plan re-analysis (docs only) | knk-workspace (docs only, no feature branch, committed straight to `main`); read-only reconciliation against knk-plugin (`ItemBlueprintsDataAccess`/`EnchantmentDefinitionsDataAccess`/`MinecraftMaterialRefsDataAccess`, task-handler taxonomy), knk-web-api (`ItemBlueprint`/`EnchantmentDefinition`/`AbilityDefinition`/`Category` models+controllers, `FormConfigurableEntity`/`RelatedEntityField` wiring, FormConfiguration seed state), knk-web-app (`itemBlueprintClient`/`enchantmentDefinitionClient`, `FormWizard`/`FormConfigBuilder`/`ManyToManyRelationshipEditor`/`WorldBoundFieldRenderer`) | 2026-09-22 | Wrote `docs/specs/items/IMPLEMENTATION_PLAN.md` (new — no prior draft existed despite the task referencing one; `docs/specs/legacy/items.md` also doesn't exist, so this has no legacy-findings source to build on either). Key reconciliation findings, all confirmed by direct code read rather than trusted from docs: (1) `ItemBlueprint`/`EnchantmentDefinition` CRUD + `[FormConfigurableEntity]`/`[RelatedEntityField]` metadata wiring is fully done on web-api and knk-plugin's data-access layer — only the web-app admin *form* (zero `FormConfiguration` exists for any item entity, no seeder mechanism exists in the codebase at all) and the in-game item-scan capability (doesn't exist in any form) are real gaps; (2) `ItemBlueprint` has no `Category`/`Grade`/pricing fields despite vision.md §9.1 calling for them — `ItemInstance` doesn't exist at all (confirmed via the code's own forward-looking TODO); (3) `IMPLEMENTATION_STATUS_AUDIT.md`/`LEGACY_VS_V2_GAP_ANALYSIS.md` (both 2026-09-10) are stale — Gate Structure Animation, which those reports call "entirely unmerged," is confirmed merged to trunk on this session's checked-out branch across all three repos, so `GATE_FORMCONFIG.md`'s field-matrix/WorldTask-bound-field pattern is live, not still-on-a-branch; (4) a real, verified regression: `m2m-join-creation-improvement-spec.md` claims inline "create a new related entity" is implemented, but `ManyToManyRelationshipEditor.tsx`'s own code path for it (`handleCreateRelatedEntity`/`ChildFormModal`) has no reachable UI trigger, contradicted by its own out-of-sync `__tests__` files. Plan proposes 5 phases (schema: add `CategoryId`; `Category`/`EnchantmentDefinition` standalone FormConfigs first since inline-create isn't reachable; `ItemBlueprint` FormConfig; new player-driven `ItemScan` `IWorldTaskHandler` — not headless like `GateBlockScanTaskHandler`, since it needs a live player holding an item; wire the scan field in + e2e verify), none blocked by other in-flight work. 7 open questions left for the developer, see the doc's §7 (Category-only vs. also Grade/Tag/pricing; FormConfig seeding mechanism — none exists yet; enchantment auto-match-on-scan; whether reading PDC is worth it yet since nothing writes it; create-only vs. re-scan-to-update; scan trigger UX; whether to fix the M2M inline-create regression as part of this or leave it). No code was written this session — docs only, per the task's own scope. **Same-day revision:** the user pointed out `docs/specs/legacy/items.md` does exist — on an unmerged branch, `legacy-spec-mining` (5 sibling per-feature legacy docs also live there, unmerged, out of this task's scope — see `docs/specs/legacy/README.md`). Cherry-picked `items.md` onto `main`. It resolves what was previously this plan's open question #1 outright: v1 (`Product`) and v2 (`Item`) both had working `Category`/`Grade`/price-range fields that v3's `ItemBlueprint` dropped, and vision.md explicitly wants them back — Phase 1 now adds `CategoryId` + a new `Grade` entity + `BasePriceMin`/`BasePriceMax`, not just `CategoryId`. Also corrected a wrong claim in the original plan: `IconMaterialRefId` already covers vision.md's "itemtype" field (it resolves the real `ItemStack` material, not just a display icon) — not a gap. Surfaced two new items: v2's `RegisterSession` (hold-an-item-and-register-its-material admin workflow) is a direct legacy precedent for the item-scan `WorldTask` design, strengthening that section; and `Tag` was never an `Item`-level concept in any version (always `Category`-scoped, and missing from `Category` in v3 too) — a separate, newly-surfaced `Category`-completeness gap, not this plan's to fix. See the plan doc's §0/§1.5/§2/§3/§7 for the full revision. **Third revision, same day:** developer explicitly decided `Tag`/`Category` enrichment and `Origin` should be in scope after all, with `Origin` specified as "a reference to either a structure, district, town, province or kingdom" (province/kingdom acknowledged not built yet). Added §3.1 (`Tag` entity + `CategoryTag` join on `Category`, matching v2's shape, not a direct `ItemBlueprint` field) and §3.2 (`ItemBlueprint.OriginId` → `Domain`, not a discriminator/polymorphic scheme — `Town`/`District`/`Structure` are already `Domain` TPT subtypes with a shared base `DbSet<Domain>`, and vision.md §2's "Kingdom, Province, Town, District are all fundamentally the same kind of thing" strongly implies `Province`/`Kingdom` will extend that same chain, meaning `OriginId` needs zero schema change once they exist). Surfaced a real technical gap this requires: `DomainsController` has no paged `/search` endpoint and `knk-web-app` has no `domainClient.ts` at all — confirmed by direct grep, zero hits either side — so the `Origin` picker needs new plumbing, not just FormConfig authoring, unlike `Category`/`Grade`/pricing. Phase 1 (§6) grew accordingly: `Tag`+`CategoryTag`+`OriginId`+the new `Domain` search endpoint/client, alongside the already-planned `CategoryId`/`Grade`/pricing. Phase 2 grew to include `Tag`'s own FormConfiguration and `Category`'s new `Tags` M2M step. 4 new open questions added (§7.8a/8b, §7.9, §7.11): whether Province/Kingdom-as-Domain-subtypes is a safe assumption, whether Origin should be single- or multi-valued, whether category-inherited tagging is really what's wanted vs. direct item-level tags, and whether `Grade`/`Tag`/`Origin` need any plugin-side representation at all (current read: no — they're admin/display metadata with no in-game rendering need in this plan's scope). See the plan doc's §0/§1.5/§2/§3/§3.1/§3.2/§4/§6/§7 for the full revision. **Fourth revision, same day: all remaining open questions walked through and resolved with the developer.** Two design decisions materially changed the plan again: (1) `Origin` is not a single FK after all — it's a chronologically-ordered collection (`ItemBlueprintOrigin` join: `Id`/`ItemBlueprintId`/`DomainId`/`SequenceNumber`), the first/oldest entry being where an item is produced and later entries reserved for future alteration-history tracking (only the production entry will actually be populated for now); (2) `Tag` is scoped at **both** `Category` (inherited) and `ItemBlueprint` (direct, new `ItemBlueprintTag` join) levels, not just `Category` as this plan previously concluded. Plugin-side representation is confirmed needed for `Grade`/`Tag`/`Origin`, including **standalone data-access gateways** (`GradesDataAccess`/`TagsDataAccess`, plus new id/search capability for `Domain`) built now rather than deferred — surfaced along the way that `knk-plugin` already has a `DomainsDataAccess` class, but it's narrowly scoped to WorldGuard-region reverse lookup only, a different responsibility from what `Origin` needs, so extending it vs. naming a new class is flagged as an implementation-time call. Also resolved: scan enchantment auto-matching (with confirmation), PDC reading (yes, defensively), scan direction (both create and update — falls out free from the generic engine), scan trigger UX (both the standard chat-claim flow and a dedicated command), FormConfig seeding (live authoring, no seeder), the M2M inline-create gap (leave it), and the premium-currency design call (confirmed settled). All 11 open questions in the plan doc's §7 are now resolved and struck through as a decision record. Plan status is now "ready for implementation." |

## Paused / blocked

| Feature | Repos / files claimed | Blocked on | Last updated |
|---|---|---|---|
| _(none currently)_ | | | |
