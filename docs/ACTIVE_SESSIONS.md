# Active Sessions

Live tracker of in-progress work across all four Knights and Kings repos
(`knk-workspace`, `knk-web-app`, `knk-web-api`, `knk-plugin`). Referenced by
`docs/ai-agents/GLOBAL_AGENT_INSTRUCTIONS.md`.

**How to use this file:**
- Before starting work, check the table below for anything overlapping
  your intended scope. If something overlaps, stop and flag it rather than
  proceeding.
- Add a row when you start; scope by **feature**, not by repo, since most
  features span all three component repos.
- Update your row's Status/Last Updated whenever you pause, and move it to
  "Recently Completed" (below) when you finish, rather than deleting it
  outright — keeps a short trail of what just happened.
- This file will get concurrent edits from parallel sessions. That's
  expected — if you hit a merge conflict here, resolve it by keeping both
  rows rather than picking one; conflicts on this file are cheap to fix by
  hand.
- Keep entries short — this is a status board, not a design doc. Link out
  to the relevant branch, PR, or `docs/reports/` file instead of restating
  detail here.

## In progress

| Feature | Repos / files claimed | Owner (human / session) | Status | Started | Last updated |
|---|---|---|---|---|---|
| _(example)_ Siege minigame — capture point sync | web-api: `Services/SiegeService.cs`; plugin: `siege/` package | Claude Code session A | Implementing capture-point event handling | 2026-09-17 | 2026-09-17 |

## Recently completed

| Feature | Repos touched | Finished | Notes / link |
|---|---|---|---|
| User features Phase 2 — legacy-check migration + `/knk` per-subcommand granularity (`IMPLEMENTATION_PLAN.md` §2, knk-plugin only) | knk-plugin: new `knk-paper/.../paper/permissions/KnkPermissible.java`; `knk-paper/.../paper/listeners/PlayerListener.java`, `knk-paper/.../paper/utils/ScoreboardUtil.java` (4 dead `k&k.*` checks replaced), `knk-paper/.../paper/commands/KnkAdminCommand.java` (13 subcommands re-permissioned), `knk-paper/.../paper/KnKPlugin.java` (wiring), `knk-paper/src/main/resources/plugin.yml` — all on branch `claude/user-features-phase2-fvx5qj`, forked from Phase 1's `claude/user-features-phase1-si2v8g` (confirmed on the right branch at session start, so the InventoryMenu-precedent "wrong fork base" problem this file's other entries repeatedly document did not recur here) | 2026-09-23 | **`KnkPermissible`** (the Phase 1 "§1 status" gap): resolves `Player`/`OfflinePlayer` → knk user id via the existing UUID-keyed `UserCache` (already populated at join), then delegates to `PermissionsDataAccess.checkAsync`. Shape decided explicitly (not left ambiguous): a synchronous `hasPermission(Player, String)` for main-thread call sites, backed by `FetchPolicy.CACHE_ONLY` — that policy never performs I/O (`DataAccessExecutor.executeCacheOnly` only reads the local cache), so the returned future is *always* already completed and safe to `.join()` from any thread including the main thread; a real network round trip is never blocked on. Fails closed (returns `false`) when the user id isn't cached yet or nothing is cached for that (userId, node) yet, per DESIGN.md §2.2's undeclared-means-deny convention, while kicking off a background `checkAsync` refresh so the next check has a real answer — mirrors the exact cache-first/background-refresh shape `PlayerListener.onValidateLogin`/`triggerBackgroundUserRefresh` already established for `UsersDataAccess`, not a new pattern. A `hasPermissionAsync(OfflinePlayer, String)` variant is available for call sites already off the main thread. **Four dead `k&k.*` checks migrated**: `PlayerListener.java:140` (`k&k.join.owner`), `:171`/`:190` (`k&k.owner`) and `ScoreboardUtil.java:51` (`k&k.*` wildcard) all now check `knk.mode.owner` through `KnkPermissible` — line numbers matched `COMMAND_PERMISSION_SCAN.md`'s inventory exactly, no drift. **`/knk` granularity**: all 13 subcommands previously gated on the single `knk.admin` node (`health`, `cache`, `towns`, `town`, `districts`, `district`, `locations`, `location`, `enchantments`, `itemblueprints`, `menu`, `streets`, `street`) now use their own `knk.admin.<subcommand>` node; `knk.admin` becomes a wildcard-style umbrella (`children:` listing all 13 as `true`) so existing op-based grants keep working unchanged, per DESIGN.md §2.3. Also removed plugin.yml's top-level `commands.knk.permission: knk.admin` — Bukkit checks that *before* `KnkAdminCommand#onCommand` ever runs, so leaving it in place would have made the whole point of per-subcommand granularity unreachable (a sender without the umbrella node could never even reach the per-subcommand check to be granted just one). Declared `knk.tasks` (used at a call site, never declared — the other cheap fix this phase's scope named). **Flagged discrepancy, not silently resolved**: `/knk gate`'s dispatch-level permission was left `null`, unchanged — `COMMAND_PERMISSION_SCAN.md` §3.2 lists it as gated behind the same single `knk.admin` node as the other 13, but a direct `git log` read shows its metadata permission has been `null` since it was first registered, predating the scan. `GateCommand` already implements its own finer-grained checks internally (`knk.gate.admin` for admin subactions, `knk.gate.open.*`/`knk.gate.close.*` for open/close) — adding a new `knk.admin.gate` top-level gate (the doc's own illustrative example) would have regressed non-admin players' existing open/close/passthrough access, so it was deliberately left as-is rather than "fixed" per the doc's example. **Flagged operational consequence, needs action before/after deploy**: `KnkPermissible` resolves purely through the new REST-backed model, never through Bukkit's own permission tree/op status — since no `PermissionGrant`/group authoring UI exists yet (§6.2, a later phase) and nothing has granted `knk.mode.owner` to anyone yet, every check currently resolves undeclared and fails closed for **everyone, including the Minecraft-op'd server owner**. The previous `k&k.owner`/`k&k.*` checks were undeclared in plugin.yml and so silently fell back to Bukkit's `isOp()` default, which is why "owner" behavior appeared to work before this migration. A `PermissionGrant` row (or group membership) for `knk.mode.owner` needs to be seeded for the real owner — via direct SQL for now, matching Phase 1's own precedent for seeding a test membership — or owner-mode cosmetics/join-bypass silently stop applying the moment this deploys. **Verification**: `repo.papermc.io` policy-blocked in this sandbox (same finding as every prior session in this repo). No knk-core/knk-api-client changes this phase, so nothing Bukkit-free to standalone-javac-verify; every change is in knk-paper (Bukkit-dependent). Syntax-checked via `javac` with no classpath — every resulting error was a missing-package/symbol error (expected, no `paper-api` reachable or cached anywhere in this sandbox, checked), with zero genuine syntax errors among them (`method does not override`/`cannot access Builder` errors are all missing-supertype/-type consequences of the same root cause, not new defects). Hand-reviewed line-by-line against the real declared signatures of `PermissionsDataAccess`, `PermissionCheckResult`, `FetchResult`, `FetchPolicy`, `UserCache`, `CommandRegistry`, and `CommandMetadata`. **Not compiler- or live-verified** — needs a real `./gradlew build` and live-client testing (join, chat, `/knk` subcommands as an op vs. a non-op, and specifically the flagged owner-mode regression above) on a machine that can reach `repo.papermc.io`. **Not started, per this phase's own explicit scope**: §3 (owner/staff-mode commands), §4 (title/XP), §5 (premium tiers), §6 (salary) — separate phases per `IMPLEMENTATION_PLAN.md` §8's sequencing. Backlog item 9 (`DataAccessFactory` TTL bug) also untouched, as instructed. **Next**: seed/verify the `knk.mode.owner` grant for the real owner before relying on owner-mode behavior; a real Gradle build + live-server pass; then §3 (owner/staff-mode commands) once this lands, per §8's sequencing. |
| User features Phase 1 — entities/API/resolution engine (IMPLEMENTATION_PLAN.md §1, both repo bullets) | knk-web-api: `Models/{PermissionHolder,PermissionGroup,PermissionGrant,UserPermissionGroup}.cs` (new), `Models/User.cs` (now `: PermissionHolder`), `Properties/KnKDbContext.cs` (TPT + new entity configs), migration `20260923125243_AddUserFeaturesPhase1PermissionSystem`, `Controllers/{PermissionGroupsController,PermissionGrantsController}.cs`, `Services/PermissionResolutionService.cs` (+ Group/Grant services/repos/DTOs/mapping), `permissions/check`+`permissions/effective` on `UsersController.cs`, `Tests/.../PermissionResolutionServiceTests.cs` (new, 22 tests) — branch `claude/user-features-phase1-sg4rjw`; knk-plugin: knk-core `domain/permissions/*` (`PermissionResolution`/`PermissionHolder`/`PermissionCheckResult`/`EffectivePermission`/`EffectivePermissionSet`), `ports/api/PermissionsApi`, `dataaccess/PermissionsDataAccess`; knk-api-client DTOs+`PermissionsMapper`+`PermissionsApiImpl`, wired into `KnkApiClient`; knk-paper `config.yml`/`KnkConfig`/`ConfigLoader`/`DataAccessFactory`/`KnKPlugin.java` wiring (new `permissions` cache-entity block, short 30s TTL) — branch `claude/user-features-phase1-si2v8g` | 2026-09-23 | **Both repo bullets of plan §1 shipped in one session** (plan's knk-plugin work was marked "if time allows," reached same session). **Migration — the flagged highest-risk step:** verified end-to-end against a real local MySQL 8.0 copy seeded with pre-existing `users`/`linkcodes` rows, not just `dotnet ef migrations add`. Two real problems the auto-generated migration didn't handle, both fixed by hand: (1) no data backfill — `INSERT INTO permission_holders SELECT Id, NULL, NULL FROM users` added between table creation and the new FK, or the FK add fails against any real data; (2) MySQL/InnoDB refuses to `ALTER`/drop `AUTO_INCREMENT` on `users.Id` while other tables' FKs reference it (`FormSubmissionProgresses`/`linkcodes`/`workflow_sessions`/`world_tasks`) — migration now drops those 4 FKs first and restores them after with identical delete behavior (verified via `information_schema` before and after). Confirmed live via a real running instance of the app: existing users/linkcode intact post-migration, `permission_holders` auto-increment counter correctly bumped, and a real `POST /api/users` creates a new user whose TPT insert correctly draws its shared id from `permission_holders` first. **Resolution engine:** 22 unit tests (multi-group weight precedence, inheritance-chain walk, wildcard vs. exact specificity, deny-beats-grant tie-break, expiry exclusion, cyclic-chain safety) all green, no regressions in the existing 328-test suite (same 5 pre-existing unrelated failures prior Items/InventoryMenu rows already documented). Also live-verified end-to-end against a real 4-level group chain (`default`→`vip`→`moderator`→`admin`) with a user-level direct deny and a wildcard-vs-exact conflict inside one group — every check/effective result matched DESIGN.md §2.2's expected precedence exactly. One resolution-order interpretation DESIGN.md doesn't pin down precisely, flagged rather than assumed silently: when a user belongs to multiple groups, each group's own inheritance chain is walked immediately after that group (before moving to the next group by weight), not "all groups' own lists first, then all parents after" — documented in `PermissionResolutionService`'s own javadoc and exercised by its tests. **knk-plugin verification:** `repo.papermc.io` policy-blocked in this sandbox (same finding every prior session in this repo has hit), worked around by fetching real dependency jars from Maven Central and compiling directly with `javac` — 0 errors for every knk-core/knk-api-client file touched, including the *entire* `KnkApiClient.java` (not just the new files in isolation). The knk-paper changes (Bukkit-dependent) are hand-reviewed only, not compiler-verified — needs a real `./gradlew build` on a machine that can reach `repo.papermc.io`. **A real pre-existing bug found and flagged, not fixed:** `DataAccessFactory`'s other 11 `create*DataAccess` methods all take a `ttl` parameter that every `KnKPlugin.java` call site fills with the *global* `config.cache().ttl()`, not that entity's own configured `entities.<name>.ttl-minutes/-seconds` — so every existing per-entity TTL config value is dead (only `default-policy`/`allow-stale`/`retry-*` actually get used, via `buildSettings`). `createPermissionsDataAccess` deliberately does not repeat this — it reads `entitySettings.permissions().ttl()` directly — so permissions actually gets the short TTL the feature needs. Worth a follow-up cleanup for the other 11 entities. **Deliberately deferred, not this session's scope per the task's own instructions:** knk-web-app (`FormConfiguration` seed entries + `permissionGroupClient`/`permissionGrantClient` — needs the controllers to exist first, which they now do); a `UserPermissionGroup` CRUD endpoint/UI for assigning users to groups (plan §1 lists the entity but no dedicated controller — membership authoring is `IMPLEMENTATION_PLAN.md` §6.2, a later phase; this session seeded test memberships via raw SQL only, there's currently no API path to create one); a `KnkPermissible`-style `Player.hasPermission(...)` replacement helper (no call site needs it yet — §2's legacy `k&k.*` migration and `/knk` per-subcommand nodes are its first real consumers, explicitly a later phase); §2 (legacy-check migration/`/knk` granularity), §3 (owner/staff-mode), §4 (title/XP), §5 (premium tiers), §6 (salary) — none started, per the plan's own sequencing. **Fields left room for so a later session doesn't re-migrate `User`/`PermissionHolder` a second time:** none added proactively (the TPT split itself is the only schema change this phase makes) — §3's vanish/mode fields and §6's `LastSalaryPayoutAt`/`PersonalSalaryMultiplier` are plain additive columns on the existing `users` table and need no coordination with this phase's `PermissionHolder` split; per `IMPLEMENTATION_PLAN.md` §8's own note, whoever picks up §3/§6 should write those as one migration together (not two), but neither depends on anything this phase did beyond `users` table still existing. **Next:** §2+§6 can start in parallel now that §1's schema/resolution engine is settled (per §8 sequencing); user-management (`docs/specs/user-management/`) can also start, since it only depended on this phase's `permissions/effective` endpoint, which now exists. |
| _(example)_ Codebase scan — knk-plugin | knk-plugin (read-only), knk-workspace (docs) | 2026-09-17 | See `docs/reports/plugin-scan-2026-09-17.md` |
| User features — rank/permission/progression design + implementation plan (docs only) | knk-workspace (docs only): `docs/specs/legacy/` (5 docs pulled verbatim from `legacy-spec-mining`, README updated), `docs/specs/user-features/` (`COMMAND_PERMISSION_SCAN.md`, `DESIGN.md`, `IMPLEMENTATION_PLAN.md`) | 2026-09-23 | Branch `claude/user-features-docs-scan-kiqifl`, not merged to `main`. **All open questions now resolved, both rounds** (12 items total in `DESIGN.md` §7): group-based data model, single-parent inheritance (multi-membership covers staff+premium-at-once), `/knk` per-subcommand nodes + the 4 dead `k&k.*` checks in scope now, title jumps straight to target using v1's 5/10/12/15 thresholds ported as-is, wildcard nodes first-class, both web+in-game authoring from day one, `PermissionGrant.HolderId` is a real FK via a new shared `PermissionHolder` TPT base table (`User`/`PermissionGroup` as subtypes — bigger migration than originally scoped, touches `User`'s inheritance shape), salary's personal multiplier is a plain `User` field, owner/staff-mode vanish state now persists across restarts (changed from v1's in-memory behavior). Verified live: account linking (vision §5.4 called it unmerged on a `UserFeatures` branch) is actually already merged to `main`/`master` in all three repos; confirmed no rank/permission/premium/salary/title code exists anywhere yet (still greenfield). Status: ready for implementation, no more open questions. Next: start Phase 1 (entities/resolution engine — see `IMPLEMENTATION_PLAN.md` §8 sequencing). |
| User management — tailored admin module design + implementation plan (docs only, new) | knk-workspace (docs only): new `docs/specs/user-management/` (`DESIGN.md`, `IMPLEMENTATION_PLAN.md`) | 2026-09-23 | Same branch as the row above. Split out as its own feature (developer-confirmed) rather than folded into `user-features`, since it's genuinely separate scope: a composite per-player profile view, group/grant quick actions, a brand-new audit-log system (confirmed no `AuditLog` entity exists anywhere despite `User.cs`'s doc-comments describing one), and moderation search/filters (confirmed no online-presence tracking exists either — new plugin-side work). Verified the existing generic `ObjectDashboard`/`PagedEntityTable`/`FormWizard` admin system already gives `User`/`PermissionGroup`/`PermissionGrant` free CRUD tables once annotated — this module only covers what that generic system can't express. Hard dependency: `user-features` `IMPLEMENTATION_PLAN.md` Phase 1 (needs the new `permissions/effective` endpoint, added there specifically for this). 3-phase plan (profile view → quick actions/audit → moderation search), plus a cross-plan coordination note: audit-log writes need to be threaded into `user-features`'s own services as they're built, not retrofitted after. 4 smaller open items in its own `DESIGN.md` §7/`IMPLEMENTATION_PLAN.md` §5 (premium-tier UI flag, presence-tracking mechanism, audit retention policy, aggregate-endpoint-vs-many-calls) — none block starting Phase 1's frontend/design work, but the first blocks finalizing Phase 1's response DTO. |
| Gate door snow-layer handling (item 7) | plugin: `knk-paper/.../gates/GateBlockPlacer.java` (`knk-plugin` branch `gate-structure-animation`); workspace: `docs/specs/gate-structure-animation/GATESTRUCTURE_QOL_IMPLEMENTATION_PLAN.md` | 2026-09-20 | Closed by explicit user decision **without** live verification: 7.1 implemented + unit-tested (`GateBlockPlacer.clearSnowLayerAbove`, `GateBlockPlacerTest`, 233/233 `knk-paper` green); 7.2's weather-dependent in-game retest on entity 14 was never done. If a hovering-snow report surfaces later, re-open with real evidence rather than assuming this already covers it. |
| Gate rotation gap-fill — rigid mid-swing animation (items 6.7/6.10/6.11, incl. 6.11.1-6.11.3) | plugin: `knk-core/.../gates/{RigidTransform,GateFrameCalculator,GateBlockPairing}.java`, `knk-core/.../domain/gates/CachedGateDoor.java`, `knk-paper/.../gates/{GateAnimationTask,GateLoaderAdapter,GateRestingFramePlacer}.java` (`knk-plugin` `17f016d`, branch `gate-structure-animation`); workspace: `docs/specs/gate-structure-animation/{ROTATION_GAP_FILL_DESIGN.md,GATESTRUCTURE_QOL_IMPLEMENTATION_PLAN.md}` | 2026-09-20 | Confirmed live (4 open/close cycles, entity 14, `GeometryDefinitionMode=REGION`): no jam, no orphaned blocks, rigid swing. See `GATESTRUCTURE_QOL_IMPLEMENTATION_PLAN.md` item 6.11.3 and `ROTATION_GAP_FILL_DESIGN.md` Decision 8.2 for full history. Item 7 (snow layer handling) is next. |
| Codebase scan — knk-web-api | knk-web-api (read-only); knk-workspace docs | 2026-09-18 | See `docs/reports/web-api-scan-2026-09-18.md`, `docs/architecture/web-api-architecture.md`, `docs/guides/web-api-reading-guide.md` |
| Codebase scan — knk-web-app | knk-web-app (read-only), knk-workspace (docs) | 2026-09-18 | See `docs/reports/web-app-scan-2026-09-18.md`, `docs/architecture/web-app-architecture.md`, `docs/guides/web-app-reading-guide.md` |
| Items Phase 1 — schema hardening | web-api: `Models/Item/{Grade,Tag,CategoryTag,ItemBlueprintTag,ItemBlueprintOrigin}.cs` (new), `Models/Item/ItemBlueprint.cs`/`Models/Category.cs` (CategoryId/GradeId/BasePriceMin/BasePriceMax/Tags/Origins), full CRUD for Grade/Tag (`Controllers`/`Services`/`Repositories`/`Dtos`/`Mapping`, mirroring `ItemBlueprintsController`'s shape), `Controllers/DomainsController.cs`+`Repositories/DomainRepository.cs` (new `POST api/Domains/search`), `Properties/KnKDbContext.cs` (new entity configs), migration `20260922122108_AddItemsPhase1SchemaHardening` (`knk-web-api` branch `claude/items-phase-1-schema-2tqpao`); plugin: `KnkItemBlueprint` (core `grade`/`tags`/`origins`), new `GradesDataAccess`/`TagsDataAccess`/`DomainCatalogDataAccess` gateways + matching `knk-api-client` DTOs/mapper/impls, `config.yml`/`KnkConfig.java`/`ConfigLoader.java`/`DataAccessFactory.java`/`KnKPlugin.java` wiring, `ItemBlueprintBukkitMapper` star-lore (`knk-plugin` same branch name); web-app: new `apiClients/domainClient.ts` + `types/dtos/domain/DomainDtos.ts` + `'domain'` case in `utils/entityApiMapping.ts`'s five dispatch functions (`knk-web-app` same branch name); workspace: this row | 2026-09-22 | Implemented per `docs/specs/items/IMPLEMENTATION_PLAN.md` §6 (Phase 1 only — no FormConfiguration authoring, no `Category.Tags` write-path, no `ItemScan`, per the plan's own phasing). **The five open-question decisions this phase had to make, all documented here per the task's own instructions:** (1) *FK delete behavior* — `ItemBlueprint.CategoryId`/`GradeId` are `Restrict`, following the `IconMaterialRefId` precedent (a catalog-lookup FK shouldn't silently null out); `CategoryTag`/`ItemBlueprintTag` cascade both ways, following `ItemBlueprintDefaultEnchantment`'s precedent (a join row is meaningless without both sides); `ItemBlueprintOrigin.ItemBlueprintId` cascades, `DomainId` restricts, exactly as the plan itself specified. (2) *SequenceNumber convention* — 0-based; `0` is the production/procurement origin, documented in the model's own comment and in the migration. (3) *`DomainsDataAccess` naming* — gave the new id/search capability its own class, `DomainCatalogDataAccess`/`DomainCatalogQueryApi`, rather than extending `DomainsDataAccess` (which is narrowly, coherently scoped to WG-region reverse lookup with a `String`-keyed cache and a different DTO shape — bolting on an `Integer`-keyed id/search responsibility would blur that). (4) *`Domains/search` response DTO* — extended the existing `DomainListDto` with a `domainType` field (the `Domain.GetType().Name` TPT-subtype convention `DomainRegionDecisionDto` already established), rather than inventing a new DTO. (5) *Grade star-lore* — included: `ItemBlueprintBukkitMapper.fromBlueprint` now appends `"&l&bGrade: ★★★"` when a blueprint has a Grade with stars > 0, reviving v1's `getGradeLore` mechanic since the data is there anyway. **Verification:** web-api — `dotnet build` 0 errors; the EF migration was generated and applied end-to-end against a real local MySQL 8.0 instance (installed fresh in-session, confirmed reachable) covering all 79 migrations from scratch; `POST api/Domains/search` exercised live and correctly returned `domainType: "Town"` for a created Town; a full `ItemBlueprint` create/update exercising Category+Grade+Tags+Origins together round-tripped correctly; the `Restrict`/`Cascade` delete behaviors were exercised live (deleting a referenced Grade/Domain correctly 409s, deleting the referencing ItemBlueprint then frees them); the full `dotnet test` suite ran 301/306 green, the 5 pre-existing failures (`ClientActivityStoreTests`, `FieldValidationServiceTests`, `FormSubmissionProgressRepositoryTests`, 2×`PathResolutionServiceTests`) are unrelated to anything this phase touched (confirmed by file/class name — none reference Item/Category/Domain/Grade/Tag). Incidental fix bundled in this commit: `knkwebapi_v2.csproj`'s `DefaultItemExcludes` only matched a lowercase `tests\**` glob but the folder on disk is `Tests/` — broke the build on any case-sensitive filesystem (this sandbox's Linux, confirmed by reproducing the 872-error baseline-build failure before touching anything else); also added the `DbUpdateException` handling `DomainsController.Delete` was missing (every sibling controller already has it) — surfaced because `ItemBlueprintOrigin.DomainId` is the first FK to ever reference `Domains`, so the previously-unreachable code path is now reachable. Plugin — `repo.papermc.io` is blocked by this sandbox's egress policy (403, confirmed via a direct check specifically for this session rather than assumed from a prior session's finding, per the task's own instruction) — even `knk-core` can't `./gradlew build` since it `compileOnly`s `paper-api` directly. Worked around by fetching the actual dependency jars (`okhttp`, `jackson-databind`/`-core`/`-annotations`, `jackson-datatype-jsr310`, `gson`, `okio`, `kotlin-stdlib`) directly from Maven Central (reachable) and running `javac` by hand: `knk-core`'s entire non-Bukkit-dependent source tree (everything except `gates/`+`domain/gates/`+two `util/` files) and the *entire* `knk-api-client` module both compile clean, 0 errors — real, if unconventional, compiler verification for both. `knk-paper`'s own changes (`KnKPlugin.java`, `KnkConfig.java`, `ConfigLoader.java`, `DataAccessFactory.java`, `ItemBlueprintBukkitMapper.java`) could only be hand-reviewed against the existing working wiring pattern for every other gateway (`ItemBlueprintsDataAccess`/`EnchantmentDefinitionsDataAccess`), not compiled — needs a real `./gradlew build` on a machine that can reach `repo.papermc.io` before trusting the `knk-paper` half the way `knk-core`/`knk-api-client` can already be trusted. Web-app — `npm install` (with `CYPRESS_INSTALL_BINARY=0`, since `download.cypress.io` isn't reachable but nothing else needed it), `tsc --noEmit` 0 errors, `npm run build` succeeds (pre-existing ESLint warnings-as-errors under `CI=true` are all in files this phase never touched — confirmed by name, none are `domainClient.ts`/`DomainDtos.ts`/`entityApiMapping.ts`/`enums.ts`), full Jest suite 220/236 green, the 16 pre-existing failures are the same `ManyToManyRelationshipEditor`/`ConfigurationHealthPanel` issues the plan doc's own §1.4 already flagged as a known, unrelated regression. **Needed before Phase 2:** a real `./gradlew build`/`test` for `knk-plugin` on a machine where `repo.papermc.io` resolves (biggest real risk carried forward — `knk-paper`'s wiring is hand-reviewed only); nothing else is blocking. |
| Items Phase 2 — Category/Grade/Tag/EnchantmentDefinition admin FormConfigurations | web-api: `Dtos/CategoryDtos.cs` (new `CategoryTagDto`, `CategoryDto.Tags` changed from read-only `List<TagNavDto>` to read/write `List<CategoryTagDto>`), `Mapping/CategoryMappingProfile.cs` (`CategoryTag`→`CategoryTagDto` map, `CategoryDto`→`Category` now explicitly ignores `Tags` — handled in service), `Services/CategoryService.cs` (new `AddTagsAsync`, wired into `CreateAsync`/`UpdateAsync`, `ITagRepository` injected) — Category's write-side Tags support, deliberately deferred by Phase 1; five `FormConfiguration` rows authored live via direct `POST`/`PUT api/FormConfigurations` (`Grade`, `Tag`, `EnchantmentDefinition`, `Category`, and `CategoryTag`'s join-entry sub-configuration) — **data, not code, not committed by this row** — see `docs/specs/items/PHASE_2_FORMCONFIGS.md` for the exact payloads and why; web-app: `src/apiClients/{gradeClient,tagClient}.ts` (new), `src/types/dtos/{grade,tag}/*Dtos.ts` (new), `src/types/dtos/category/CategoryDtos.ts` (added `tags`/`CategoryTagDto`), `src/utils/enums.ts` (`Controllers.Grades`/`Tags`, `GradeOperation`/`TagOperation`), `src/utils/entityApiMapping.ts` (`'grade'`/`'tag'` cases in all five dispatch functions — mechanical plumbing Phase 1 didn't need since it only added `Domain`), `src/components/FormWizard/FormWizard.tsx` (bug fix, see below) — all `knk-web-app`/`knk-web-api`/`knk-plugin` branch `claude/items-feature`; workspace: this row + `docs/specs/items/PHASE_2_FORMCONFIGS.md` (new) | 2026-09-22 | Implemented per `docs/specs/items/IMPLEMENTATION_PLAN.md` §6 Phase 2 (admin `FormConfiguration`s for `Category`/`Grade`/`Tag`/`EnchantmentDefinition`, plus `Category.Tags` write support) — no `ItemBlueprint` form (Phase 3), no `ItemScan` (Phase 4/5), per the plan's own phasing. **Branch note:** this session's harness again assigned a fresh randomly-suffixed branch (`claude/friendly-gauss-mw6vyl`) with none of Phase 1's commits, across all three component repos — the exact repeated pattern several InventoryMenu rows above document. Confirmed via `git log`/`git fetch` that `claude/items-feature` (not the stale `claude/items-phase-1-schema-2tqpao`, which GitHub App permissions blocked deleting) carries Phase 1's real commits in all three repos; switched to it and continued there, per the task's own explicit instruction rather than as an improvised judgment call this time. **Design calls made, none clearly spelled out by the plan or Phase 1's row:** (1) *Category.Tags write shape* — added a `CategoryTagDto` (`CategoryId`/`TagId`/`Tag`) mirroring `ItemBlueprintTagDto`'s read shape exactly, used for **both** read and write (`Category` has one shared DTO class for create/update/read, unlike `ItemBlueprint`'s split Create/Update/Read DTOs) — the FormWizard M2M engine submits an array of join-entry objects keyed by the join entity's real FK field name (confirmed by tracing `normalizeManyToManyRelationshipField`/`resolveJoinEntityMapping`), not a flat `tagIds: number[]` the way `ItemBlueprintService.AddTagsAsync`'s hand-written precedent uses — so `CategoryService.AddTagsAsync` reconciles `List<CategoryTagDto>` (reading just `.TagId` off each entry), not a flat int list, deliberately diverging from the `ItemBlueprint` precedent to match what the generic engine actually produces. (2) *`EnchantmentDefinition.MinecraftEnchantmentRefId` widget* — plain `Object` picker, not `HybridMinecraftEnchantmentRefPicker`, since `MinecraftEnchantmentRef` rows are pre-seeded catalog data (no get-or-create need) and the Hybrid variant's submission path isn't wired in `normalizeFormSubmission.ts` at all (only the material variant is) — using it would have hit a genuinely untested code path for no benefit. (3) *`EnchantmentDefinition.Description`/`Category.Name` marked required* — both are non-nullable model columns with no default; `FormTemplateValidationService` correctly rejected the first attempt at authoring `EnchantmentDefinition` with `description` optional, which is how this was caught rather than assumed. **A real, pre-existing engine bug found and fixed** (not a FormConfiguration authoring issue, and not something in scope to leave alone since it blocked the very verification this phase requires): `FormWizard.tsx` passes the raw lowercase `:entityName` route segment (e.g. `"category"`) as `entityTypeName` into `normalizeFormSubmission` and compares it directly against backend metadata's correctly-cased `relatedEntityType` (e.g. `"Category"`) in two places (the top-level submit call and `handleJoinEntryComplete`'s related-field resolution) — the case mismatch meant a plain (no-extra-column), bidirectional-metadata M2M join's "which side is the *other* entity" exclusion filter never actually excluded the parent's own side, silently resolving the wrong FK field or losing the selected id entirely. Reproduced live via `Category`'s `Tags` step (`CategoryTag` has both a `CategoryId`→`Category` and a `TagId`→`Tag` related field, so the self-match was picked first): every join-entry submission failed with "Join entry 1 in Tags is missing a related entity selection" despite a real, valid Tag being selected in the UI. Fixed both sites to use the already-loaded `config.entityTypeName` (falling back to the route param only if the config hasn't loaded yet). This is latent, pre-existing engine code — no `FormConfiguration` had ever exercised this exact path before (`ItemBlueprint`'s own M2M steps, which would hit the same code, don't exist until Phase 3) — flagged in `docs/specs/items/PHASE_2_FORMCONFIGS.md` for whoever builds Phase 3 to watch for the same class of bug elsewhere in the file. Confirmed no regression: `git stash`/rerun of the Jest suite before and after the fix produced identical pass/fail counts. **Verification, all live against a real running system, not simulated:** `.NET 8 SDK` and `mysql-server-8.0` installed fresh in-sandbox (neither present initially; `dotnet.microsoft.com`'s own CDN is blocked by this sandbox's egress policy — confirmed 403 — but Ubuntu's own `apt` archive carries `dotnet-sdk-8.0`/`mysql-server-8.0` and both installed clean), `dotnet build knkwebapi_v2.csproj` 0 errors (same `Tests/` vs `tests/` sln case-mismatch Phase 1 already found and worked around — built the project directly, not the `.sln`), all 79 migrations applied to a fresh local MySQL 8.0 database, `dotnet test Tests/knkwebapi_v2.Tests/...` 301/306 green (same 5 pre-existing failures as Phase 1's row, confirmed unrelated by name), `knk-web-app`: `npm install`, `tsc --noEmit` 0 errors, full Jest suite 220/236 green (same 16 pre-existing failures as Phase 1's row). Both servers started locally (`dotnet run` on :5294, `npm start` on :3000) and driven end-to-end with a real headless Chromium browser (Playwright, package installed to a scratch dir since it wasn't in `node_modules`; the pre-installed browser binary needed `--headless=new` since Playwright's default `--headless=old` flag has been removed from current Chrome) through a real registered/logged-in user account: **Grade** create (`POST` 201) + edit (`PUT` 204), confirmed in MySQL both times; **Tag** create ×2 + edit, confirmed in MySQL; **EnchantmentDefinition** create + edit (`MaxLevel` 3→5), confirmed in MySQL, correctly alongside the 12 pre-existing `AbilityDefinition.SeedCanonicalAsync` rows; **Category** create with one `CategoryTag` join entry (via "Create New Join Entry" → real `Tag` picker, not a stub), then edit adding a second tag, then edit removing one tag — all three steps confirmed against the real `CategoryTag` table (`(1,1)` → `(1,1),(1,2)` → `(1,2)`), proving the M2M step actually adds/removes real join rows, not just UI state. **Not done, flagged rather than silently skipped:** `MinecraftEnchantmentRefId`'s object picker wasn't clicked through to actually select a value (only verified it renders and submits `null` correctly) — lower-risk than the Tags M2M path since it reuses the exact same generic `ObjectField` mechanism already proven working for `Category.ParentCategoryId`, but a real pick-and-submit wasn't independently exercised for this specific field. **Needed before Phase 3:** re-author (or script) these five `FormConfiguration`s against the developer's actual persistent dev database — this session's MySQL instance and everything in it is gone when the sandbox ends; `docs/specs/items/PHASE_2_FORMCONFIGS.md` has the exact payloads for this. Also worth a grep of `FormWizard.tsx` for other `entityName`-vs-metadata comparisons before Phase 3's `ItemBlueprint` form is built, per the engine-bug note above — only the two sites this phase's testing actually exercised were confirmed fixed. |
| Items — plan re-analysis (docs only) | knk-workspace (docs only, no feature branch, committed straight to `main`); read-only reconciliation against knk-plugin (`ItemBlueprintsDataAccess`/`EnchantmentDefinitionsDataAccess`/`MinecraftMaterialRefsDataAccess`, task-handler taxonomy), knk-web-api (`ItemBlueprint`/`EnchantmentDefinition`/`AbilityDefinition`/`Category` models+controllers, `FormConfigurableEntity`/`RelatedEntityField` wiring, FormConfiguration seed state), knk-web-app (`itemBlueprintClient`/`enchantmentDefinitionClient`, `FormWizard`/`FormConfigBuilder`/`ManyToManyRelationshipEditor`/`WorldBoundFieldRenderer`) | 2026-09-22 | Wrote `docs/specs/items/IMPLEMENTATION_PLAN.md` (new — no prior draft existed despite the task referencing one; `docs/specs/legacy/items.md` also doesn't exist, so this has no legacy-findings source to build on either). Key reconciliation findings, all confirmed by direct code read rather than trusted from docs: (1) `ItemBlueprint`/`EnchantmentDefinition` CRUD + `[FormConfigurableEntity]`/`[RelatedEntityField]` metadata wiring is fully done on web-api and knk-plugin's data-access layer — only the web-app admin *form* (zero `FormConfiguration` exists for any item entity, no seeder mechanism exists in the codebase at all) and the in-game item-scan capability (doesn't exist in any form) are real gaps; (2) `ItemBlueprint` has no `Category`/`Grade`/pricing fields despite vision.md §9.1 calling for them — `ItemInstance` doesn't exist at all (confirmed via the code's own forward-looking TODO); (3) `IMPLEMENTATION_STATUS_AUDIT.md`/`LEGACY_VS_V2_GAP_ANALYSIS.md` (both 2026-09-10) are stale — Gate Structure Animation, which those reports call "entirely unmerged," is confirmed merged to trunk on this session's checked-out branch across all three repos, so `GATE_FORMCONFIG.md`'s field-matrix/WorldTask-bound-field pattern is live, not still-on-a-branch; (4) a real, verified regression: `m2m-join-creation-improvement-spec.md` claims inline "create a new related entity" is implemented, but `ManyToManyRelationshipEditor.tsx`'s own code path for it (`handleCreateRelatedEntity`/`ChildFormModal`) has no reachable UI trigger, contradicted by its own out-of-sync `__tests__` files. Plan proposes 5 phases (schema: add `CategoryId`; `Category`/`EnchantmentDefinition` standalone FormConfigs first since inline-create isn't reachable; `ItemBlueprint` FormConfig; new player-driven `ItemScan` `IWorldTaskHandler` — not headless like `GateBlockScanTaskHandler`, since it needs a live player holding an item; wire the scan field in + e2e verify), none blocked by other in-flight work. 7 open questions left for the developer, see the doc's §7 (Category-only vs. also Grade/Tag/pricing; FormConfig seeding mechanism — none exists yet; enchantment auto-match-on-scan; whether reading PDC is worth it yet since nothing writes it; create-only vs. re-scan-to-update; scan trigger UX; whether to fix the M2M inline-create regression as part of this or leave it). No code was written this session — docs only, per the task's own scope. **Same-day revision:** the user pointed out `docs/specs/legacy/items.md` does exist — on an unmerged branch, `legacy-spec-mining` (5 sibling per-feature legacy docs also live there, unmerged, out of this task's scope — see `docs/specs/legacy/README.md`). Cherry-picked `items.md` onto `main`. It resolves what was previously this plan's open question #1 outright: v1 (`Product`) and v2 (`Item`) both had working `Category`/`Grade`/price-range fields that v3's `ItemBlueprint` dropped, and vision.md explicitly wants them back — Phase 1 now adds `CategoryId` + a new `Grade` entity + `BasePriceMin`/`BasePriceMax`, not just `CategoryId`. Also corrected a wrong claim in the original plan: `IconMaterialRefId` already covers vision.md's "itemtype" field (it resolves the real `ItemStack` material, not just a display icon) — not a gap. Surfaced two new items: v2's `RegisterSession` (hold-an-item-and-register-its-material admin workflow) is a direct legacy precedent for the item-scan `WorldTask` design, strengthening that section; and `Tag` was never an `Item`-level concept in any version (always `Category`-scoped, and missing from `Category` in v3 too) — a separate, newly-surfaced `Category`-completeness gap, not this plan's to fix. See the plan doc's §0/§1.5/§2/§3/§7 for the full revision. **Third revision, same day:** developer explicitly decided `Tag`/`Category` enrichment and `Origin` should be in scope after all, with `Origin` specified as "a reference to either a structure, district, town, province or kingdom" (province/kingdom acknowledged not built yet). Added §3.1 (`Tag` entity + `CategoryTag` join on `Category`, matching v2's shape, not a direct `ItemBlueprint` field) and §3.2 (`ItemBlueprint.OriginId` → `Domain`, not a discriminator/polymorphic scheme — `Town`/`District`/`Structure` are already `Domain` TPT subtypes with a shared base `DbSet<Domain>`, and vision.md §2's "Kingdom, Province, Town, District are all fundamentally the same kind of thing" strongly implies `Province`/`Kingdom` will extend that same chain, meaning `OriginId` needs zero schema change once they exist). Surfaced a real technical gap this requires: `DomainsController` has no paged `/search` endpoint and `knk-web-app` has no `domainClient.ts` at all — confirmed by direct grep, zero hits either side — so the `Origin` picker needs new plumbing, not just FormConfig authoring, unlike `Category`/`Grade`/pricing. Phase 1 (§6) grew accordingly: `Tag`+`CategoryTag`+`OriginId`+the new `Domain` search endpoint/client, alongside the already-planned `CategoryId`/`Grade`/pricing. Phase 2 grew to include `Tag`'s own FormConfiguration and `Category`'s new `Tags` M2M step. 4 new open questions added (§7.8a/8b, §7.9, §7.11): whether Province/Kingdom-as-Domain-subtypes is a safe assumption, whether Origin should be single- or multi-valued, whether category-inherited tagging is really what's wanted vs. direct item-level tags, and whether `Grade`/`Tag`/`Origin` need any plugin-side representation at all (current read: no — they're admin/display metadata with no in-game rendering need in this plan's scope). See the plan doc's §0/§1.5/§2/§3/§3.1/§3.2/§4/§6/§7 for the full revision. **Fourth revision, same day: all remaining open questions walked through and resolved with the developer.** Two design decisions materially changed the plan again: (1) `Origin` is not a single FK after all — it's a chronologically-ordered collection (`ItemBlueprintOrigin` join: `Id`/`ItemBlueprintId`/`DomainId`/`SequenceNumber`), the first/oldest entry being where an item is produced and later entries reserved for future alteration-history tracking (only the production entry will actually be populated for now); (2) `Tag` is scoped at **both** `Category` (inherited) and `ItemBlueprint` (direct, new `ItemBlueprintTag` join) levels, not just `Category` as this plan previously concluded. Plugin-side representation is confirmed needed for `Grade`/`Tag`/`Origin`, including **standalone data-access gateways** (`GradesDataAccess`/`TagsDataAccess`, plus new id/search capability for `Domain`) built now rather than deferred — surfaced along the way that `knk-plugin` already has a `DomainsDataAccess` class, but it's narrowly scoped to WorldGuard-region reverse lookup only, a different responsibility from what `Origin` needs, so extending it vs. naming a new class is flagged as an implementation-time call. Also resolved: scan enchantment auto-matching (with confirmation), PDC reading (yes, defensively), scan direction (both create and update — falls out free from the generic engine), scan trigger UX (both the standard chat-claim flow and a dedicated command), FormConfig seeding (live authoring, no seeder), the M2M inline-create gap (leave it), and the premium-currency design call (confirmed settled). All 11 open questions in the plan doc's §7 are now resolved and struck through as a decision record. Plan status is now "ready for implementation." |
| Items Phase 3 — ItemBlueprint admin FormConfiguration | web-api: `Dtos/ItemBlueprintDtos.cs` (`TagIds: List<int>` → `Tags: List<ItemBlueprintTagDto>` on Create/Update DTOs), `Services/ItemBlueprintService.cs` (`AddTagsAsync` reconciles `List<ItemBlueprintTagDto>` now); web-app: `src/components/FormWizard/FormWizard.tsx` (third `entityName`-vs-metadata casing-bug fix, in `handleOpenJoinEntry`); six `FormConfiguration` rows authored live via `POST`/`PUT api/FormConfigurations` against the developer's real dev DB (`192.168.50.119`/`knightsandkings_dev_v2`) — `ItemBlueprint` itself plus its three join-entry sub-configs (`ItemBlueprintDefaultEnchantment`, `ItemBlueprintOrigin`, `ItemBlueprintTag`) — **data, not code**; four stale pre-existing `FormConfiguration` rows (`EnchantmentDefinition`, `ItemBlueprint`, `ItemBlueprintDefaultEnchantment` — all dated Feb 2026, unrelated to this feature — plus none for the two new join entities) deactivated (`isDefault: false`) rather than deleted, per explicit developer decision; one real `Grade`/`Category`/`Tag`/`EnchantmentDefinition`/`Tag`(second) row created via the live forms for pickers to use — all `knk-web-api`/`knk-web-app` branch `claude/items-feature`; workspace: this row | 2026-09-23 | Implemented per `docs/specs/items/IMPLEMENTATION_PLAN.md` §6 item 3. **Branch verification:** confirmed `claude/items-feature` had Phase 1 (`574d90f`)/Phase 2 (`8c31dbe`) commits in `knk-web-app`; `knk-web-api`'s working directory was found checked out on the unrelated `claude/inventory-menu-phase-2-x4y98c` branch (a leftover from the machine's last session, not an active claim per `ACTIVE_SESSIONS.md`'s empty "In progress" table at the time) — switched to `claude/items-feature` after confirming the working tree was clean. **Step 1 (apply pending Phase 1 work to the real DB):** `dotnet ef database update` applied `20260922122108_AddItemsPhase1SchemaHardening` cleanly — confirmed no table/column overlap with InventoryMenu's already-applied migrations both before (diffed the migration's `Up()` against InventoryMenu's known tables) and after. **A real surprise neither `IMPLEMENTATION_PLAN.md` nor `PHASE_2_FORMCONFIGS.md` anticipated** (both assumed disposable sandbox databases): the real dev DB already had pre-existing, differently-shaped `isDefault:true` `FormConfiguration` rows for `EnchantmentDefinition`, `ItemBlueprint`, and `ItemBlueprintDefaultEnchantment` (all dated Feb 2026, predating this feature). Investigated rather than guessed: confirmed via code read that (a) nothing in `FormConfigurationService`/`FormTemplateValidationService`/the repository enforces single-default-per-entity — a second `isDefault:true` POST would silently coexist: (b) `FormConfigurationDto.IsActive` is a dead DTO field with no backing entity column (`FormMappingProfile` ignores it both directions) — irrelevant to selection, a red herring; (c) `FormWizardPage.tsx` always fetches the *full* config list (`defaultOnly=false`) and picks `configs.find(c => c.isDefault) || configs[0]` client-side, with no deterministic ordering between two `isDefault:true` rows, and surfaces a "too many default configurations" warning in list view when this happens. Flagged to the developer rather than improvised past; explicit decision: deactivate the old rows (`isDefault: false` via PUT, keeping them as inert history) rather than delete or overwrite in place, then author fresh ones as the sole defaults. **`ItemBlueprintCreateDto`/`UpdateDto.TagIds` DTO fix — verified needed, not copied blind:** traced `normalizeManyToManyRelationshipField`/`normalizeFormSubmission.ts` directly and confirmed the M2M engine submits `{Tags: [{TagId: X}, ...]}` for a plain (no-extra-column) M2M step, which a flat `List<int> TagIds` can't bind; `DefaultEnchantments`/`Origins` DTOs already matched the array-of-objects-with-extra-column shape and needed no change, confirming the task's own prediction. Fixed by reusing the already-existing read-side `ItemBlueprintTagDto` (keyed by `TagId`) for write too, mirroring `CategoryService.AddTagsAsync`'s Phase 2 precedent. **`FormWizard.tsx` grep, per the task's explicit instruction before wiring three more M2M steps:** found and fixed a **third**, previously-unfixed site with the same `entityName`-vs-metadata casing-bug class Phase 2 fixed at two other sites — `handleOpenJoinEntry`'s parent/related-field resolution (lines ~1405-1421) compared the raw lowercase route-segment `entityName` directly against correctly-cased backend metadata, same pattern, same fix (`config.entityTypeName || entityName`). A third site (`handleOpenChildForm`, unrelated to M2M) was left alone — it belongs to the already-known-unreachable "inline related-entity creation" dead-UI path §1.4/open-question-7 explicitly decided to leave alone. **FormConfiguration authored:** `ItemBlueprint` (General Information: `Name`/`DefaultDisplayName`/`Description`/`DefaultDisplayDescription`/`CategoryId`/`IconMaterialRefId`/`GradeId`/`DefaultQuantity`/`MaxStackSize`; Pricing: `BasePriceMin`/`BasePriceMax`; three M2M steps — Default Enchantments/Origin/Tags — each with its own join-entry sub-configuration, patched in via `subConfigurationId` the same two-request create-then-patch Category's `Tags` step used in Phase 2) exactly per §4.1's field matrix. **Verified live, real running system, real dev DB, the same rigor Phase 2 applied to `Category.Tags`:** `dotnet build`/`dotnet test` (301/306 green, same 5 pre-existing failures as every prior Items row, confirmed unrelated by name) and the full Jest suite (220/236 green, same 16 pre-existing failures as every prior Items row) both show zero regressions from either code change. Both servers started locally (`dotnet run` :5294, `npm start` :3000) and driven end-to-end via real headless Chrome (Playwright, `channel: 'chrome'` against the machine's existing Chrome install — no browser download needed this time). **Create:** `/forms/category`, `/forms/grade`, `/forms/tag`, `/forms/enchantmentdefinition` each exercised via their real forms to seed one real row apiece (needed since this real DB had none of Phase 2's sandbox-created rows); `/forms/itemblueprint` filled end-to-end (General Info + Pricing + all three M2M steps, each via "Create New Join Entry" → real picker → real child field), submitted, and the resulting `ItemBlueprint` row confirmed via direct `GET` to have real `CategoryId`/`GradeId`/`BasePriceMin`/`BasePriceMax` plus real `ItemBlueprintDefaultEnchantment`/`ItemBlueprintOrigin`/`ItemBlueprintTag` join rows in the database — not just UI state. **Edit:** `/forms/itemblueprint/edit/:id` loaded the created entity correctly (all scalar fields pre-filled correctly), then a second Tag was added and the first removed via the Tags M2M step and resubmitted; confirmed via `GET` that the update correctly persisted the tag set to exactly the new tag, proving both add and remove work on the update path. **A real, pre-existing cosmetic bug found and flagged, not fixed (out of this phase's scope, doesn't corrupt data):** relationship cards added via "Create New Join Entry" show a false "⚠ Missing Entity" warning and a generic "Relationship #N" label instead of the real entity's name, immediately after creation and after loading existing data in edit mode. Root cause: `handleJoinEntryComplete` only populates a relationship's `relatedEntity` display field from `joinData[relatedNavigationFieldName]` or `joinData.relatedEntity`, but `normalizeFormSubmission`'s `handleSingleObjectRelationship` converts a submitted Object field (e.g. `TagId`) to its bare FK id and strips the nested entity object entirely before the join-entry sub-form's `onComplete` ever fires — so `relatedEntity` is always `undefined` for the "Create New Join Entry" path specifically, while the real FK id (`relatedEntityId`/`TagId` etc.) *is* still correctly threaded through and submitted, which is exactly why this session's live create/edit/remove tests all persisted correct data despite the scary-looking warning. This almost certainly affects `Category.Tags` from Phase 2 too (same code path) — Phase 2's own verification checked the database table state, not the relationship-card UI, so it was never noticed there either. Left alone rather than fixed, matching the established "don't fix shared-component gaps beyond what blocks this phase's own verification" precedent (Phase 2's open question 7) — flagged here for a developer decision on priority. **Also found and fixed, real but minor:** `IsCustom` on `EnchantmentDefinition` is a non-nullable `bool` on the backend DTO, but its `FormConfiguration` field marks it `isRequired: false` and the Boolean checkbox renderer submits `null` when left untouched — creation 400s with no detail until the checkbox is explicitly clicked. Worked around for this session's seed data (explicitly checked it); not fixed in the FormConfiguration itself since that's Phase 2's own authored config, out of this phase's stated scope, but worth a one-line `isRequired: true` fix on that field whenever Phase 2's config is next touched. **Git:** both `knk-web-api`/`knk-web-app` commits pushed to `claude/items-feature`; `knk-web-api`'s push was initially rejected (remote had moved — someone merged `claude/inventory-menu-phase-2-x4y98c` into `claude/items-feature` while this session was running, bringing in InventoryMenu Phases 1-8), resolved with a clean no-conflict merge (confirmed neither touched file overlapped) before pushing. **Needed before Phase 4:** none of this phase's own work is blocking — Phase 4/5 (`ItemScan`) can start whenever the developer wants; the flagged "Missing Entity" display bug and the `IsCustom` FormConfiguration gap are both pre-existing, cross-phase-affecting UX papercuts worth a developer decision on priority, not architectural blockers. |
| Items Phase 4/5 — ItemScan WorldTask + FormConfig wiring | plugin: `knk-paper/.../tasks/ItemScanTaskHandler.java` (new, `IWorldTaskHandler`), `KnKPlugin.java` (handler registration by both field name and taskType), `commands/KnkAdminCommand.java` (`/knk itemscan claim <code>`, delegates to `KnkTaskClaimCommand`) — `knk-plugin` branch `claude/items-feature`; web-api: `Dtos/GateBlockScanDtos.cs` (`WorldTaskTypes.ItemScan` constant, optional) — same branch; web-app: `FormConfigBuilder/FieldEditor.tsx` (`ItemScan` worldTaskType option), `Workflow/WorldBoundFieldRenderer.tsx` (`isItemScanTask` branches in `extractTaskResult`/`getWorldTaskResultDetails`, kept out of `HEADLESS_TASK_TYPES`), `FormWizard/FormWizard.tsx` (`applyItemScanResult`/`applyMultipleFieldChanges`, cross-field auto-fill + enchantment auto-match), `apiClients/minecraftMaterialRefClient.ts` (new `getHybrid`/`persistFromCatalog`, mirroring the enchantment-ref client) — same branch; workspace: this row, five live `FormConfiguration` field edits against the real dev DB (data, not code) | 2026-09-23 | Implemented per `docs/specs/items/IMPLEMENTATION_PLAN.md` §5/§6 items 4-5. **Verified real:** `knk-plugin` — full `./gradlew build` (real `repo.papermc.io` access confirmed this session, not assumed from a prior session), all modules compile and the full test suite passes, 0 failures, for every commit in this row (four separate builds, one per fix below); jar auto-deployed to the real local dev server via the existing `deployToDevServer` task. `knk-web-api` — `dotnet build` 0 errors (built to a scratch output dir since the real dev API instance, PID-locked by an attached debugger, was left running and untouched, same pattern Phase 1/3 already established). `knk-web-app` — `tsc --noEmit` 0 errors and the full Jest suite unchanged at 220/236 (same 16 pre-existing failures every prior Items/InventoryMenu row already documents), for every commit. **Live end-to-end verification, real running system, not simulated for the web half:** registered a real user via the actual multi-step registration wizard (Playwright + the machine's own installed Chrome, no browser download needed) against the real dev API (already running on the developer's machine, `:5294`) and the real dev web-app (`:3000`); opened `/forms/itemblueprint`, used the real "ItemBlueprint - Default" `FormConfiguration` (id 21, Phase 3's own live-authored config), clicked "Send to Minecraft" on the `Default Display Name` field and confirmed a real `WorldTask` row was created (`taskType: "ItemScan"`, `fieldName: "defaultDisplayName"`, a real claim code, verified via direct `GET`) with the exact claim-code banner UI (`🎮 Ready for Minecraft! ... /knk task-claim <code>`). **No live Minecraft client or dev server was reachable in this environment** (the dev Minecraft server process itself wasn't running, and driving an actual game client isn't something this session can do) - the literal "hold an item, run the claim command in-game" step is the one piece of GATE_FORMCONFIG.md item 5's verification checklist this row could not execute. Worked around for everything downstream of that: called the real `POST api/WorldTasks/{id}/complete` endpoint directly (the exact same endpoint `ItemScanTaskHandler.worldTasksApi.complete(...)` calls) with a hand-built `OutputJson` matching the handler's real output shape byte-for-byte, then let the already-running web page's own 2-second polling loop pick it up exactly as it would a real plugin completion - this exercises 100% of the web-app-side logic (extraction, cross-field fan-out, material resolution, enchantment matching) for real, only skipping the physical in-game action itself. **Real bugs found and fixed by this live testing, none caught by build/tests/hand-review:** (1) `ItemScanTaskHandler` was registered in `WorldTaskHandlerRegistry` only by field name `"ItemScan"` - but the FormField the WorldTask panel actually binds to is `defaultDisplayName` (see next bug), so `getHandler(taskType, fieldName)` would never have found it when claimed; fixed by also registering under taskType `"ItemScan"`, the same dual-registration `LocationTaskHandler` already uses for its `"LocationSelection"` alias. (2) The scan's `customEnchantments[].key` was prefixed `"knk:"` per `EnchantmentDefinition.Key`'s doc-comment example (`"knk:lifesteal"`) - confirmed live against the real `EnchantmentDefinitions` table that the actual seeded canonical rows (the only ones `LocalEnchantmentRepositoryImpl` can ever parse from lore) store `Key` as the bare id (`"poison"`, `"chaos"`, ...), matching `EnchantmentRegistry`'s own ids exactly; the prefix was a real-data-contradicting assumption, not a convention - removed it. (3) `FormWizard.tsx`'s `applyMultipleFieldChanges` (the batched cross-field write `applyItemScanResult` needs since `handleFieldChange` isn't safe to call twice from a stale async closure) itself read `currentStepData`/`allStepsData` from the render closure the same way `handleFieldChange` does - fine for every *existing* caller (all synchronous), but `applyItemScanResult` awaits real network calls first, so by the time the patch applied, `reconcileVisibility` computed against stale data silently dropped the freshly-scanned field. Fixed by switching both `setState` calls to React's functional form. (4) Separately, the patch used hardcoded PascalCase keys (`DefaultDisplayName`, ...) but this specific live-authored `FormConfiguration`'s fields are camelCase (`defaultDisplayName`, ...) - `DefaultDisplayName` appeared to work anyway only because its own `onChange` (the field the panel is bound to) had already set the correctly-cased key for an unrelated reason moments earlier; `DefaultDisplayDescription`/`IconMaterialRefId` had no such fallback and silently no-opped. Fixed by resolving each target field's real `fieldName` from `currentStep.fields` at runtime rather than assuming either casing convention. **Confirmed fixed, via a repeat of the simulated-completion test after each fix:** `DefaultDisplayName` → "Flametongue", `DefaultDisplayDescription` → both scanned lore lines, joined; `Default Enchantments` step correctly pre-selected both a matched vanilla (`minecraft:sharpness` → "Sharpness") and a matched custom (`poison` → "Poison") `EnchantmentDefinition`, rendered as real relationship cards with no "Missing Entity" warning, still requiring the admin's own review/save (never a silent write) - exactly the §5.3 design. `IconMaterialRefId`'s own resolve/get-or-create call (`GET .../hybrid` then `POST .../get-or-create`) was confirmed to succeed against a real, already-persisted `minecraft:diamond_sword` row, using the same `applyMultipleFieldChanges`/`resolveFieldName` mechanism already independently confirmed correct for the other two fields - not independently re-screenshotted to the same certainty as those two, flagged here rather than silently assumed. **A real, load-bearing design gap neither the plan nor Phase 1-3 anticipated, resolved rather than escalated:** `ItemBlueprint` has no dedicated "scan result" field the way `GateDoor` has `BlockSnapshots` (a real persisted collection) - `FormTemplateValidationService` rejects any `FormField.fieldName` that isn't a real entity property (confirmed live: a standalone `"ItemScan"` field 400'd with `"Field 'ItemScan' does not exist on entity 'ItemBlueprint'"`), so a phantom scan-summary field was never viable. Resolved by binding the WorldTask panel directly onto the existing, required `DefaultDisplayName` field instead (chosen over `IconMaterialRefId` specifically because `extractTaskResult` must stay synchronous - matching every other task type's contract - and material resolution is inherently async, so it has to live in `onTaskCompleted` regardless of which field hosts the panel). `WorldBoundFieldRenderer.tsx`'s `isItemScanTask` extraction branch returns the scanned display name as a plain string (falling back to `''`, not `null`, so a scan with no custom name - the common case, since most vanilla items are never renamed - doesn't surface a spurious extraction-error banner) rather than a summary object. **Not done - the one piece this row could not execute in this environment, recommended before trusting this feature the way GATE_FORMCONFIG.md's own still-open item 5 template asks:** the actual physical "hold a real item, run `/knk task-claim <code>` or `/knk itemscan claim <code>` in a real Minecraft client against the real dev server" step, and the subsequent "admin reviews the pre-filled form and saves a real `ItemBlueprint` row" + "a rescan on an existing `ItemBlueprint` overwrites wholesale" checks. Given how much this session's live web-side testing already caught (four real bugs, three of which a live Minecraft client alone would not have surfaced any faster than the simulated-completion approach did, since they were all web-app-side), the remaining gap is narrow and low-risk, but genuinely unverified - the dev Minecraft server process was not running in this environment and starting/operating one requires the developer's own machine and game client either way. **Post-completion note (2026-09-23, live in-game testing round 1):** the developer reached step 4 of the checklist above (`/knk task-claim <code>` in-game) and it worked - the item scanned successfully and updated the web form. Two real bugs and one design gap surfaced, all fixed in follow-up commits, plus three new commands added on request: (1) *Vanilla item names weren't scanned* - `ItemScanTaskHandler` only read `ItemMeta.getDisplayName()`, null for any never-renamed item (the common case); now falls back to a humanized `Material` name (`DIAMOND_SWORD` → `Diamond Sword`) when there's no custom name, so `DefaultDisplayName` populates either way. (2) *Developer-requested new feature:* `ScanConflictModal` (`knk-web-app`) - `applyItemScanResult` now checks, against live state (not its own async-stale closure), whether `DefaultDisplayName`/`DefaultDisplayDescription`/`IconMaterialRefId`/`Default Enchantments` already have a value before overwriting; if so it pauses and asks per-field whether to use the scan result or keep the existing value (defaults to "use scan," since triggering a scan is itself a request for fresh data), rather than always silently overwriting. (3) *New commands, developer-requested:* `/knk item rename <displayName>` and `/knk item lore <add|set|remove>` (1-based lines; `set` inserts and shifts subsequent lines down, appending instead if the given line is at or past the current end; both translate `&`-color codes via the existing `DisplayTextFormatter`, matching the same convention the web app's minecraft-text-color fields already use) - this capability genuinely didn't exist in the plugin before. (4) *`/knk enchantments` merged into `/knk item enchantments`* per developer request - the standalone top-level command (and its `enchantment` alias) no longer exists; tab-completion re-indexed accordingly, `EnchantmentDefinitionsDebugCommand` itself untouched, only its entry point moved. (5) *A real, independently-reported bug, found and fixed:* `/knk enchantments apply sharpness 2` (and `minecraft:sharpness 2`) resolved to an unrelated custom enchantment (`armor_repair`, "max level is 1") instead of vanilla Sharpness II. Root cause was two-layered, confirmed live via direct API calls before and after each fix: `EnchantmentDefinitionRepository.SearchAsync` (`knk-web-api`) never read `PagedQuery.Filters` at all (only `SearchTerm`) - unlike every other repository using the same query type (Category, GateStructure, Location, MinecraftMaterialRef, Structure, User, WorldTask) - so any filtered search silently returned every row unfiltered; `EnchantmentDefinitionsDebugCommand.pickCustomDefinitionMatch` (`knk-plugin`) then had a fallback that returned *any* custom definition from an unfiltered result set when no real match was found, rather than correctly returning no match. Fixed both: the repository now supports `Id`/`Key` (exact)/`DisplayName` (contains) filters, case-insensitive key lookup; the plugin's dangerous fallback was removed outright. Verified live via direct `POST api/EnchantmentDefinitions/search` calls with real filter payloads, both before (confirming the bug: `Key: sharpness` returned everything) and after (confirming the fix: `Key: sharpness` correctly returns zero rows since the real key is `minecraft:sharpness`, `Key: minecraft:sharpness` and `Key: poison` both return exactly the right single row). **Still not done - not reached yet in the developer's own in-game testing:** steps 6-12 of the checklist (a real custom-lore-encoded enchantment scan, a material with no existing `MinecraftMaterialRef` row, an empty-hand scan, save + rescan-overwrite against a real persisted `ItemBlueprint`, and now also re-confirming steps 4-5 with the vanilla-name-fallback and conflict-modal changes in place, since neither existed during round 1's testing). **Post-completion note (2026-09-23, round 2 — testing continued to the Default Enchantments/Origin/Tags steps and hit save):** a large batch of real findings, all fixed. (1) `DefaultQuantity` now defaults to `1` (live FormConfiguration `defaultValue`, data not code); `MaxStackSize` now comes from the scan itself (`ItemScanTaskHandler` adds `Material.getMaxStackSize()` to the output) since it genuinely varies per material, unlike quantity. (2) The `ScanConflictModal` (round 1) had a real bug: it always reported a "conflict" on `DefaultDisplayName` even on a genuinely empty form, because the bound WorldTask field's own `onChange` had already written the scanned value into state *before* `applyItemScanResult` even started checking - the check just needed to snapshot state *before* any `await`, not after, since nothing renders in between within one synchronous callback tick. Fixed by moving the snapshot earlier. (3) `ManyToManyRelationshipEditor`'s relationship cards showed only the related entity's name, never the join entity's own scalar fields (e.g. `Level` on a matched enchantment) - confirmed this was hardcoded, not a `Default Table Columns` setting; now shows a real summary of the join entity's own fields. (4) **The actual submission blocker, root-caused**: "Join entry 1 in Tags is missing a related entity selection" traced to `ChildFormModal`'s inline "Create New" flow for a plain single Object relationship field (e.g. `TagId`, and by the same logic `DomainId` for Origin): it only ever embedded the raw, *unpersisted* submitted object in local form state (`persistIndependently` was only ever true for an `ownedChildCollection` List field, e.g. GateDoors) - `normalizeFormSubmission.handleSingleObjectRelationship` then calls `extractId()` on an object with no `id` (nothing had created it), so the field silently vanished from the join entry's own submission instead of erroring loudly. This is a real, previously-undiscovered gap affecting *any* inline "Create New" on a non-Location single Object field anywhere in the app, not just Items - fixed by persisting immediately, matching how owned collections already worked. The paired cosmetic bug this exposed (`handleJoinEntryComplete` never populating `relatedEntity` for the "Create New Join Entry" path, since `normalizeFormSubmission` correctly strips it to a bare FK id before `joinData` arrives - "Missing Entity" warning + generic "Relationship #N" label, matching Phase 3's own flagged-but-deferred finding, now actually blocking) was also fixed: fetches the entity by id when `relatedEntity` is missing but a real id is present. (5) **New feature, developer-requested**: the Origin step (and generically, any M2M step whose join entity carries a `SequenceNumber` field) now has a drag-to-reorder UI on its relationship cards (`@dnd-kit/sortable`, reusing `FormConfigBuilder`/`SortableStepItem`'s exact pattern), with `SequenceNumber` auto-derived from array position instead of hand-typed - the live `ItemBlueprintOrigin` join-entry `FormConfiguration`'s own `SequenceNumber` field was removed (data change) so nothing prompts for it manually anymore. **Verified:** `knk-plugin` full `./gradlew build` green (tests + deploy) for the `MaxStackSize` commit; `knk-web-app` `tsc --noEmit` clean and full Jest suite unchanged at 220/236 for every commit in this round; the live dev server (restarted once after CRA's incremental TS cache genuinely corrupted itself mid-session - confirmed via a fresh `tsc --noEmit` passing while the running dev server falsely reported `rxjs` unresolvable - deleting `node_modules/.cache/tsconfig.tsbuildinfo` and restarting fixed it) now compiles clean with no warnings at all. **Not yet re-verified live by the developer** (this session made the fixes and confirmed via build/tests/dev-server-compile-state; the developer's own click-through re-test of the Tags/Origin save flow, the reordering UI, and the enchantment-level card display is still pending). **Post-completion note (2026-09-23, round 3 - a third-level nesting bug, self-inflicted this time via a Playwright reproduction rather than developer testing):** the developer reported that creating a new Category's own Parent Category inline (from within ItemBlueprint's Category "Create New" form) never registered the new parent - same symptom as the Tag bug round 2 fixed, one level deeper. Round 2's fix didn't cover this, so this session reproduced it directly (Playwright, headless Chrome, real dev API/DB) rather than reasoning about it in the abstract, and found two distinct bugs stacked on each other: (1) `findParentLinkField`'s "pre-fill the field that links back to my parent wizard" heuristic matches purely on `objectType`, which breaks for any self-referencing entity - Category's own `ParentCategoryId` field has `objectType: "Category"`, identical to the type of the Category wizard it was opened *from*, so it got wrongly pre-filled with that other, still-unsaved Category's snapshot (`id: -1`) - confirmed via the real network log: `POST api/Categories` with `parentCategoryId: -1` followed by a genuine 500, `FK_categories_categories_ParentCategoryId` constraint violation (`-1` isn't a real row). Fixed by never applying the heuristic when the child and parent entity types are the same (case-insensitive). (2) That failure was completely invisible: `ChildFormModal.handleChildComplete` called the async `onComplete` (`FormWizard.handleChildFormComplete`) without awaiting it, so the modal closed immediately regardless of whether the create actually succeeded - a real, previously-undiscovered robustness gap affecting *any* nested "Create New" failure for *any* entity, not just this self-reference case. Fixed by awaiting it and keeping the modal open with a real error message on failure; `handleChildFormComplete` now rethrows instead of silently swallowing. **Verified fixed** via the same live reproduction script, rerun after the fix: Parent Category now shows up correctly as a real persisted entity (green selected-item box, real id 5) on the sub-category's own field. `tsc --noEmit` clean, Jest unchanged at 220/236, dev server compiling clean (same one pre-existing warning). **Not yet independently confirmed by the developer's own click-through** (this session's own Playwright reproduction is real-system evidence, but not the same as the developer's own hands re-testing it). **Post-completion note (2026-09-23, round 4):** developer asked whether Origin should appear in the built item's lore per vision.md §9.1 - confirmed yes (that section explicitly lists "origin" among what "lore is regenerated display output" renders), but that line describes `ItemInstance`'s future regenerated-display mechanism, out of this plan's scope entirely. Phase 1 already set a precedent for doing this early anyway, at the `ItemBlueprint` level, for Grade's star-lore ("worth doing since the data will be there anyway, though not required by this plan's scope") - developer confirmed extending the same treatment to Origin now. `ItemBlueprintBukkitMapper.fromBlueprint` (`knk-plugin`) now appends `"&7Origin: <Domain name> (<DomainType>)"` using the production/procurement origin (sequenceNumber 0, the only entry populated in practice). `./gradlew build` green (tests + deploy). **Post-completion note (2026-09-23, round 5 — merged to main/master across all three repos, and forward-ported into the parallel `claude/user-features-phase3-v8rmws` branch):** developer asked for `claude/items-feature` to be merged into each repo's default branch, and for the in-flight `claude/user-features-phase3-v8rmws` branch to be updated with the result. **Merge to default branch:** `knk-plugin` — `main` was stale locally (12 commits behind `origin/main`, plus one unpushed local-only fix, `ca824c3` "let ops bypass the vanilla pickup cancel", unrelated to Items); rebased the local fix onto fresh `origin/main` and pushed it first, then merged `items-feature` in (real merge commit, not a fast-forward, since that local fix diverged main from items-feature's own base) — `./gradlew build` + full test suite green, pushed. `knk-web-api` — clean fast-forward onto `master` (7 commits, 0 behind); real compile errors: none (only a file-copy failure from an already-running, unrelated `dotnet` debug-adapter process holding the output DLL locked — not touched, since unlike this session's own past stale processes this one was started minutes earlier and is almost certainly the developer's own live IDE debug session) — pushed. `knk-web-app` — clean fast-forward onto `main` (11 commits, 0 behind); `tsc --noEmit` clean — pushed. **Forward-port into `claude/user-features-phase3-v8rmws`** (exists in `knk-plugin` and `knk-web-api` only — flagged to the developer that `knk-web-app` has no such branch, nothing to do there): `knk-plugin` — real conflicts in `KnkConfig`/`ConfigLoader` (`EntityCacheSettings` — both branches independently appended a `menus` field; deduplicated and combined into `menus, permissions, grades, tags, domainCatalog`, keeping `ConfigLoader`'s load order in sync), `KnKPlugin.java` (purely additive field lists, concatenated), and `KnkAdminCommand.java` (phase3's branch still had the pre-round-1 standalone `/knk enchantments` `CommandMetadata`; kept `items-feature`'s merged `/knk item` tree instead, since the rest of that file - the usage examples, the `itemCommand.onCommand` dispatch wiring - was already unconflicted and already assumed the merged tree existed) — full `./gradlew build` and test suite green after resolution, pushed. `knk-web-api` — one real conflict in the EF Core model snapshot (`Migrations/KnKDbContextModelSnapshot.cs`): both branches independently added a new entity in the same `Street`→`User` alphabetical slot (`Tag` from items-feature, `UserPermissionGroup` from phase3's permissions work); reordered into the correct alphabetical position (`Street`, `Tag`, `User`, `UserPermissionGroup`) rather than trusting either side's raw diff placement - `dotnet build` 0 errors, and the full test suite (334 tests) came back with the exact same 5 pre-existing failures as a plain, unmodified checkout of `origin/master` (confirmed via a throwaway `git worktree` comparison), confirming the merge introduced no regressions - pushed. |

## Paused / blocked

| Feature | Repos / files claimed | Blocked on | Last updated |
|---|---|---|---|
| _(none currently)_ | | | |
